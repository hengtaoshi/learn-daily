# 云计算转行 6 个月执行手册

> 使用者：计算机专业本科 + 实施工程师 14 个月，云计算零基础，工作场景用不到
> 可用时间：工作日 2h/天，周末 4~6h/天
> **总投入**：18~22h/周。**但其中每晚 40min 记笔记 + 10min 复盘，真正"动手"只有 10~11h/周，26 周约 270~290 小时。** 后面所有排期按**动手时间**算，不按"坐着的时间"算。
> 本地配置：Windows + 24GB 内存 + 原生 Ubuntu（100G）

---

## 目录

- [第 0 步：先回答一个问题](#第-0-步先回答一个问题)
- [第一部分：环境准备清单](#第一部分环境准备清单)
- [第二部分：时间与节奏设计](#第二部分时间与节奏设计)
- [第三部分：6 个月总览](#第三部分6-个月总览)
- [第四部分：逐月逐周逐日执行表](#第四部分逐月逐周逐日执行表)
- [第五部分：检查点体系](#第五部分检查点体系)
- [第六部分：时间不够时的取舍规则](#第六部分时间不够时的取舍规则)
- [第七部分：必然踩到的坑与解法](#第七部分必然踩到的坑与解法)
- [第八部分：投入预算](#第八部分投入预算)
- [第九部分：两个必补模块](#第九部分两个必补模块)
- [第十部分：求职目标修正](#第十部分求职目标修正)
- [附录：学习资源清单](#附录学习资源清单)

---

## 第 0 步：先回答一个问题

> **这一个问题会直接影响后面三处做法，必须先答。**

**你的 Ubuntu 是虚拟机，还是物理双系统？**

| 维度               | 虚拟机（VMware / VirtualBox / Hyper-V） | 物理双系统                 |
| ---------------- | ---------------------------------- | --------------------- |
| 能否与 Windows 同时运行 | **能**                              | 不能                    |
| 边看视频边敲命令         | 可以，互不干扰                            | 必须重启切换                |
| 网络形态             | NAT（默认）或桥接（桥接后等同局域网主机）             | 直接就是局域网主机             |
| **快照回滚**         | **有**，搞坏了 30 秒回滚                   | 没有，坏了只能救援或重装          |
| 最危险的练习           | 无                                  | W2「故意改坏 sudoers」、分区操作 |
| 学习真实性            | 约 90%（内核、systemd、网络栈都是真的）          | 100%                  |

**结论：如果还没定，优先选虚拟机。** 理由不是省事，是**快照**——这套计划里有一半高价值练习是"故意搞坏再修好"，双系统下搞坏 sudoers 或引导是真实风险，虚拟机下是一个按钮的事。

**如果已经是物理双系统**：不影响学习质量（而且更真实），但两条前置动作必做：

1. 1.5 的 7 项设置全部做完；
2. **先做 1.8 的备份与救援准备**，再开始 W2 的破坏性练习。

> 下文 1.5 及"验证方式"章节写明**双系统分支**。若你是虚拟机，1.5 可整体跳过，改为 1.5-A（虚拟机的 4 个优势用法）。

---

## 第一部分：环境准备清单

**先花 1~2 天把环境一次性搭好，不要边学边搭。** 环境没搭好就开始学，90% 的人会在第三周放弃。

### 1.0 环境形态决策：Windows 与 Ubuntu 分工

**结论：动手实操全部放原生 Ubuntu，Windows 只做"看、写、管"。**

原生 Ubuntu **比 WSL2 更适合这套学习**，原因是真实的：

| 维度                      | 原生 Ubuntu              | WSL2                   | 胜出          |
| ----------------------- | ---------------------- | ---------------------- | ----------- |
| 文件系统性能                  | 原生 ext4，无损耗            | `/mnt/d` 跨系统慢 10 倍     | **原生**      |
| Docker 运行               | 原生 Engine，cgroup v2 完整 | 嵌套虚拟化，偶发怪异问题           | **原生**      |
| K8s 集群                  | k3d 跑得干净               | 依赖 Docker Desktop 的 VM | **原生**      |
| iptables / nftables     | 完整可用，可自由折腾             | 与 Windows 网络栈耦合，坑多     | **原生**      |
| systemd                 | 原生（托管服务/写 unit）        | 默认关闭，需额外配置             | **原生**      |
| 关掉图形界面省内存               | 可以，纯文本省 1~2GB          | 不适用                    | **原生**      |
| **真实度**                 | **等同生产服务器**            | 接近但非等同                 | **原生**      |
| 看视频/写文档/开阿里云控制台         | 要装一堆东西                 | Windows 原生体验           | **Windows** |
| 云文档、截图、Obsidian、IDE 图形化 | 一般                     | 好                      | **Windows** |

**所以分工是：**

| 任务                                                     | 在哪做                        |
| ------------------------------------------------------ | -------------------------- |
| Linux / 网络抓包 / Docker / K8s / 脚本 / Terraform / Ansible | **原生 Ubuntu**              |
| 看教程视频、写笔记文档、查资料、开阿里云控制台、提交简历                           | **Windows**                |
| VS Code 写代码                                            | 两边都行（Ubuntu 里装 VS Code 更顺） |

**最重要的一点：在原生 Ubuntu 里你是真的在生产形态下工作。** 面试被问"你平时用什么环境"时，"我在原生 Linux 上做实验"比"我用 WSL"更有说服力。

### 1.1 Ubuntu 侧必装（主力环境）

```bash
sudo apt update && sudo apt install -y \
  curl wget git vim tmux tree jq unzip shellcheck \
  net-tools dnsutils iputils-ping traceroute mtr-tiny \
  tcpdump telnet netcat-openbsd \
  htop iotop sysstat lsof strace \
  build-essential python3 python3-pip python3-venv \
  ca-certificates gnupg lsb-release
```

> 1. **原清单漏装了 `shellcheck`**，但 1.1 的表格和 W9 D6 都要用它。已补。
> 2. **`nmap` 已从"必装"移到"可选"**。它是扫描工具，本身合法，但**只能扫你自己的资产**——扫别人的网段在国内属违法行为，别在咖啡馆随手跑。需要时再装：`sudo apt install -y nmap`。

**装完必须做的两步（很多人卡在这里）**：

```bash
# ① sysstat 装着不等于在采数据。不开启的话 sar 查历史永远是空的，你会以为它坏了
sudo sed -i 's/^ENABLED="false"/ENABLED="true"/' /etc/default/sysstat
sudo systemctl enable --now sysstat

# ② iotop 需要 root 才能看到全部进程的 IO，以后记得 sudo iotop
```

| 类别                       | 软件                              | 用途             | 何时用        |
| ------------------------ | ------------------------------- | -------------- | ---------- |
| 终端复用                     | **tmux**                        | 断线不丢会话，开多窗口做实验 | 每天都用       |
| 编辑器                      | VS Code（Linux 版）或 vim           | 写脚本、YAML       | 每天都用       |
| 容器                       | **Docker Engine**（见 1.2）        | 容器实验           | 第 4 月      |
| 编排                       | kubectl、**k3d**、Helm            | K8s 实验         | 第 4 月      |
| 抓包                       | tcpdump（命令行）+ Wireshark（可选 GUI） | 网络分析           | 第 2 月      |
| 网络工具                     | dig / mtr / nc                  | 排障             | 第 2 月      |
| 性能工具                     | htop / iotop / sysstat / strace | 排障             | 第 1 月      |
| Python                   | python3 + pip + venv            | 自动化            | 第 3 月      |
| 代码检查                     | shellcheck、pylint               | 脚本质量           | 第 3 月      |
| Terraform                | 官方源安装                           | IaC            | 第 6 月      |
| Ansible                  | `apt install ansible`           | 配置管理           | 第 6 月      |
| **Nginx**                | `apt install nginx`             | 反代/HTTPS/LB    | **第 2 月末** |
| **Prometheus + Grafana** | docker compose                  | 监控             | **第 5 月**  |

**Wireshark 的说明**：原生 Ubuntu 上可以直接装 Wireshark GUI，抓包比 Windows 更干净。但第 2 月建议**先用 tcpdump 抓、再用 Wireshark 分析 pcap 文件**——这才是服务器上的真实用法。

### 1.2 Ubuntu 侧关键配置（5 步，缺一不可）

**① Docker Engine 安装（不要装 Docker Desktop for Linux）**

```bash
# 方式 A：官方脚本（最省事，但要接受"把远端脚本直接喂给 shell"）
curl -fsSL https://get.docker.com | sudo sh

# 方式 B（运维应该用的方式）：官方 apt 源，可审计、可复现
sudo install -m 0755 -d /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | \
  sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg
sudo chmod a+r /etc/apt/keyrings/docker.gpg
echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] \
https://download.docker.com/linux/ubuntu $(. /etc/os-release && echo $VERSION_CODENAME) stable" | \
  sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
sudo apt update && sudo apt install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

```bash
# 把当前用户加入 docker 组，免 sudo
sudo usermod -aG docker $USER
# 然后必须重新登录（或 newgrp docker）才生效
```

> **安全认知（面试会问）**：`docker` 组等价于 root。因为能起特权容器的人，挂载宿主 `/` 就拿到了 root shell。生产环境里"给某人加 docker 组"是一张 root 通行证，不是"方便一下"。

**② 配置镜像加速 + 日志轮转（防磁盘被日志吃满）**

```bash
sudo mkdir -p /etc/docker
sudo tee /etc/docker/daemon.json <<'EOF'
{
  "registry-mirrors": [
    "https://<你的专属ID>.mirror.aliyuncs.com",
    "https://docker.m.daocloud.io"
  ],
  "log-driver": "json-file",
  "log-opts": { "max-size": "50m", "max-file": "3" }
}
EOF
sudo systemctl daemon-reload && sudo systemctl restart docker
docker info | grep -A3 "Registry Mirrors"   # 验证生效
```

> **下面这几个"通用"加速地址现在都不靠谱，别照抄：**
> 
> - `dockerproxy.com` —— 已基本失效/转为只读，连不上是常态；
> - `mirror.ccs.tencentyun.com` —— **只在腾讯云内网的 CVM 上稳定**，家用宽带上很慢，你不是腾讯云机器，别用；
> - `docker.m.daocloud.io` —— 可以当备胎，但不如厂商专属地址稳。
> 
> **正确做法（免费）**：登录阿里云「容器镜像服务 ACR」控制台 → 创建**个人实例** → 左侧「镜像加速器」→ 复制**你专属的** `https://xxxx.mirror.aliyuncs.com`。它绑定你的账号、有独立带宽配额，比任何公共源都稳。**注意这串 ID 是专属的，不能抄别人的。**
> 
> 三条补充规则：
> 
> 1. **只配 1 主 1~2 备，别堆五六个。** Docker 按顺序尝试，源不通时要等超时才切下一个，堆多了失败等待会叠加。
> 2. **`registry-mirrors` 只代理 pull，不代理 push。** push 到 Docker Hub 慢是另一回事，配了加速器也不会变快。
> 3. **`log-opts` 只对新建容器生效**，已存在的容器不会变。以后排查"磁盘被日志吃满"要记得这条。
> 
> 备选源（可用性会变，以你实测为准）：中科大 `docker.mirrors.ustc.edu.cn`、`docker.1ms.run`（公益源，晚高峰会慢）。
> **镜像源会失效，失效就重测，别在拉镜像上耗超过 30 分钟。**

**③ swap 策略**

内存 24G 但分给 Ubuntu 多少取决于安装方式。先查：

```bash
free -h          # 看实际分到多少内存
swapon --show    # 看 swap 情况
```

- 内存 ≥ 8G：把 swappiness 调低（性能偏好，不是必须）：
  
  ```bash
  echo 'vm.swappiness=10' | sudo tee /etc/sysctl.d/99-ops-learn.conf
  sudo sysctl --system
  ```

- 内存 < 8G：保留 swap，否则 OOM 会很频繁。

> **注意：调低 swappiness 并不能解决 K8s 的 swap 问题，这两件事没有因果关系：**
> 
> - 真正的 K8s（kubelet）默认 `--fail-swap-on=true`，**节点开着 swap 会直接启动失败**，不是调低 swappiness 能解决的。真要跑 kubelet，得 `sudo swapoff -a` 并从 `/etc/fstab` 注释掉 swap 行。
> - 但本计划用的是 **k3d**，集群节点跑在容器里、由宿主 Docker 管内存，**不依赖宿主 swap 状态**，所以第 4 月不用为此折腾。知道这个区别就行，面试爱问。
> - 另外写 sysctl 用 `/etc/sysctl.d/99-xxx.conf` 是现在的规范位置，`/etc/sysctl.conf` 是遗留文件。

**④ SSH 服务装好并固定 IP（这一条最容易漏，但一定会用到）**

第 3 月的巡检脚本、第 5 月验证跨机连通、第 6 月 CI/CD 部署，都需要"能被连上"。

```bash
sudo apt install -y openssh-server
sudo systemctl enable --now ssh
ip addr        # 记下自己的 IP
```

- 如果是**虚拟机**：默认 NAT 模式下外部（含 Windows 主机、云服务器）连不进来。要跨机访问就改成**桥接（Bridged）**；只在 Windows 本机验证则用 NAT + 端口转发即可。
- 如果是**双系统**：记下局域网 IP，并在路由器里**绑定静态 IP 或 DHCP 保留**，否则重启换 IP 后你的脚本和配置全部失效。

> 配好密钥登录后，云上机器请顺手加固——`PermitRootLogin no`、`PasswordAuthentication no`、安全组不要对 `0.0.0.0/0` 长期开放 22。学习机无所谓，但这是面试常问的"你会怎么加固一台公网机器"。

**⑤ 确认系统信息（先搞清楚你在什么环境里）**

```bash
cat /etc/os-release   # 比 lsb_release 更可靠，lsb-release 包在精简安装里可能没有
uname -r              # 内核版本
nproc && free -h && df -h /    # CPU / 内存 / 磁盘
```

> **为什么要确认版本**：Ubuntu 26.04 是**今年 4 月才发布的新 LTS**（现在才 5 个月），内核较新，Docker 和 K8s 整体没问题，但第三方教程、社区脚本、某些厂商 Agent 可能还没跟上。
> 遇到"集群网络诡异不通""iptables 规则看着对但不生效"，**先怀疑新内核与 nftables/iptables 兼容层，而不是怀疑自己**。搜索时把版本号和内核号带上，能省大量时间。
> 另注：cgroup v2 和 nftables 早就是 Ubuntu 默认了（不是 26.04 的新变化），把这个当"新版本特性"去找原因是方向错的。

### 1.3 100G 磁盘：够用，但要管理

**结论：100G 够跑完这 6 个月，但必须有清理习惯，否则第 4 月就会满。**

空间大致消耗：

| 项目                       | 占用                       |
| ------------------------ | ------------------------ |
| Ubuntu 系统本体              | 15~25G                   |
| Docker 引擎 + 基础镜像         | 5~10G                    |
| 构建缓存（最容易失控）              | 10~40G                   |
| k3d/k3s 集群 + 镜像          | 5~15G                    |
| **自建 GitLab（不推荐，见 W22）** | **15~30G，且持续增长**         |
| 代码、日志、其他                 | 5~10G                    |
| **合计**                   | **40~100G（含 GitLab 会超）** |

**维护动作（每周日复盘时顺手做一次）：**

```bash
df -h /                       # 先看还剩多少
docker system df              # 看 Docker 各部分占用
docker system prune           # 清停止的容器/无用网络/悬空镜像（安全）
docker builder prune -af      # 清构建缓存（占空间大头，最该清）
docker image prune -a         # 清未被使用的镜像（谨慎：清完要重新拉）
k3d cluster delete <name>     # 不用集群就删掉，别留着
sudo journalctl --vacuum-size=200M    # 按大小清
sudo journalctl --vacuum-time=7d      # 或按时间清，效果更直观
```

> 注意 `docker system prune -a` 会把**所有未使用镜像（含基础镜像缓存）**一起清掉，在你镜像源不稳时，代价是下次构建要重新拉几百 MB 甚至失败。**分工具清理更可控**，优先级是"构建缓存 > 悬空镜像 > 未使用镜像"。

**如果安装方式是虚拟机**：虚拟磁盘扩容比较容易，满了再加即可（甚至可以直接在线扩）。
**如果是双系统物理分区**：扩容麻烦（尤其是分区顺序靠后的情况），所以更要从第一周就养成清理习惯。**现在就去确认你的安装方式和剩余空间。**

### 1.4 Windows 侧保留什么

Windows 不要卸载，它承担四件事：

| 用途                     | 工具                          |
| ---------------------- | --------------------------- |
| 看教程视频、查资料              | 浏览器                         |
| 写笔记、复盘、架构图             | Obsidian / Typora / draw.io |
| **开阿里云控制台**（第 5-6 月常用） | 浏览器                         |
| 投简历、面试                 | 浏览器 + 微信/钉钉                 |

**不要做的事**：不要在 Windows 上装 Docker Desktop 再学一遍。**你已经有了更好的环境，别搞两套。**

### 1.5 双系统专属：7 个必做设置

> 仅当你选择**物理双系统**时适用。虚拟机用户直接跳到 1.5-A。

**双系统的最大限制：两个系统不能同时运行。** 这条会改变几件事的做法，下面逐条处理。

#### ① 【最重要】关闭 Windows 快速启动和休眠

不关的话，Windows 会"锁住"NTFS 分区，**Ubuntu 只能只读挂载 Windows 盘，你没法在 Linux 里读写 Windows 的文件**。

```powershell
# 管理员 PowerShell
powercfg /h off
```

再到「控制面板 → 电源选项 → 选择电源按钮的功能 → 更改当前不可用的设置」，**取消勾选"启用快速启动"**。

> 如果启用了 BitLocker，也要先确认恢复密钥再操作，否则改引导设置会有风险。**这一步现在做，不要等到第 3 月写脚本要用 Windows 文件时才发现。**

#### ② 解决时间错乱（一定会遇到）

> **为什么不推荐 `timedatectl set-local-rtc 1`：** 它是把硬件时钟改成读"本地时间"，systemd 自己会打警告说它"不完全受支持"，而且会在夏令时/跨时区场景引入难查的偏差。
> 
> **推荐做法：统一把硬件时钟当 UTC 用**（Linux 本来就是这么设计的），在 **Windows** 侧改一行注册表：

```powershell
# 管理员 PowerShell（Windows 侧）
reg add "HKLM\System\CurrentControlSet\Control\TimeZoneInformation" /v RealTimeIsUniversal /t REG_DWORD /d 1 /f
```

```bash
# Ubuntu 侧确认是 UTC 模式（默认就是，确认一下）
timedatectl set-local-rtc 0
timedatectl          # 期望看到 RTC in local TZ: no
```

> 这个方案能工作的前提正是**第 ① 步已经关掉快速启动/休眠**——Windows 休眠时会用 RTC 写盘，两者叠加才会出现"睡一觉时间又乱了"。若你偶尔需要休眠 Windows，那就退回 `set-local-rtc 1` 并接受它的局限。

#### ③ 缩短 GRUB 等待时间

默认 GRUB 等 5 秒或 30 秒，每次开机都浪费。在 Ubuntu 里：

```bash
sudo vim /etc/default/grub
# 改：GRUB_TIMEOUT=3
# 想默认启动 Windows 就设 GRUB_DEFAULT=2（先用 grep menuentry /boot/grub/grub.cfg 确认序号）
sudo update-grub
```

#### ④ 【关键】磁盘空间：现在就去确认分区结构

```bash
lsblk -f
df -h /
```

**关键判断：100G 分区在磁盘的什么位置？**

| 情况                          | 能不能扩容   | 怎么做                                  |
| --------------------------- | ------- | ------------------------------------ |
| 分区**在磁盘末尾**，后面无其他分区         | **容易**  | Windows 磁盘管理里压缩相邻分区，再把空闲空间分配给 Ubuntu |
| 分区**在磁盘中间**，后面还有 Windows 分区 | **很麻烦** | 需要移动分区，风险高，**不要轻易尝试**                |
| 装 Ubuntu 时选了"与 Windows 共存"  | 通常是这种   | 按上一条处理                               |

**更聪明的方案（避免动分区）：** 从 Windows 的 D 盘或 E 盘压缩出一块**未分配空间**，在 Ubuntu 里格式化为 ext4，挂到 `/var/lib/docker` 或 `~/projects`。

```bash
lsblk -f                      # 先确认设备名，别搞错盘（重点：确认没有挂载点、型号对得上）
```

> **⚠️ 风险提醒**
> `mkfs.ext4` 是**不可逆的数据销毁操作**。设备名写错一位，毁的是你的 Windows 分区。执行前必须同时确认三件事：① `lsblk -f` 里该分区**没有挂载点**；② 容量与你刚压缩出的空间一致；③ 系统盘型号/序号对得上。
> 稳妥做法：从 `/dev/disk/by-id/` 下的软链接引用设备，比 `/dev/nvme0n1pX` 更防呆。

```bash
# ⚠️ 确认无误后再执行
sudo mkfs.ext4 /dev/nvme0n1pX
sudo mkdir -p /var/lib/docker
sudo mount /dev/nvme0n1pX /var/lib/docker
blkid /dev/nvme0n1pX          # 取 UUID
# 写进 /etc/fstab 永久生效（用 UUID，不要用设备名）
echo 'UUID=xxxx /var/lib/docker ext4 defaults 0 2' | sudo tee -a /etc/fstab
sudo mount -a && df -h /var/lib/docker   # 验证 fstab 没写错，否则重启会进 emergency mode
```

> **⚠️ 顺序错了会出事**：如果你在 1.2 先装了 Docker（数据已写入 `/var/lib/docker`），到 1.5④ 才把新分区挂上去——**这样挂载会把原有数据整个盖住**，表现为"Docker 突然起不来"或"镜像全没了"。
> **正确顺序二选一：**
> 
> - **推荐**：先做分区分区/挂载（1.5④），再装 Docker（1.2①）；
> 
> - **已经装过 Docker 了**：
>   
>   ```bash
>   sudo systemctl stop docker docker.socket containerd
>   sudo mkdir -p /mnt/newdocker
>   sudo mount /dev/nvme0n1pX /mnt/newdocker
>   sudo rsync -aAX /var/lib/docker/ /mnt/newdocker/   # 迁移现有数据
>   sudo umount /mnt/newdocker
>   # 再按上面的方式挂到 /var/lib/docker，然后 systemctl start docker
>   ```

> 另有两个容易忽略的现实约束：
> 
> 1. **ext4 分区 Windows 之后完全看不见、用不了。** 这块空间只能给 Linux，不能两边共用。别想着"顺便存点 Windows 文件"。
> 2. **如果磁盘是 MBR 分区表且 4 个主分区已占满，你压不出第 5 个分区。** 先 `sudo fdisk -l` 看分区表类型，MBR 满了就只能走"扩 Ubuntu 分区"或换硬盘这条路。

**双系统下扩容后要做的检查：** 只有在**Linux 写过 NTFS 分区之后**，回 Windows 才需要跑 `chkdsk /f`。
（注意：单纯压缩卷 + 对未分配空间做 `mkfs.ext4`，不碰 NTFS 元数据，不需要检查。真正的风险来自"同一块 NTFS 被两个系统轮流挂载写入"。）

#### ⑤ 【关键】改变"验证方式"：不要在 Windows 里验证

双系统下两个系统不能同时开，所以这些做法**行不通或很别扭**：

| 原来的做法                         | 双系统下的问题 | 改成                                                                                           |
| ----------------------------- | ------- | -------------------------------------------------------------------------------------------- |
| 从 Windows 浏览器打开 Ubuntu 的 8080 | 要重启才能看  | **在 Ubuntu 里 `curl -I localhost:8080`**                                                      |
| 从 Windows 浏览器访问 K8s Ingress   | 同上      | 在 Ubuntu 的 `/etc/hosts` 加 `127.0.0.1 myapp.local`，然后 `curl --resolve` 或直接 `curl myapp.local` |
| 从 Windows ssh 进 Ubuntu        | 同上      | 在 Ubuntu 本机 `ssh localhost` 验证服务                                                             |

**习惯的转变：在服务器上你本来就没有浏览器。** 用 `curl`、`curl -v`、`wget`、`nc` 验证服务，这才是在 P0 环境里真实的工作方式。这一步反而让你更接近生产。

> **第 5 月"跨机验证"怎么做？**
> **先说一个想当然的错法**："从你的阿里云 ECS 上直接 `ssh` 或 `nc -zv` 连你本地 Ubuntu 的局域网 IP"——**这做不到**。你本地是 `192.168.x.x` 私网地址，而 ECS 在公网，公网机器**永远无法主动访问一个 NAT 后面的私网 IP**。
> 
> **正确的三种做法（按推荐度）：**
> 
> 1. **SSH 反向隧道（推荐，零成本、零第三方，而且本身就是 SSH 隧道练习的正统延伸）**
>    
>    ```bash
>    # 云服务器侧：允许隧道监听公网
>    sudo sed -i 's/^#\?GatewayPorts.*/GatewayPorts yes/' /etc/ssh/sshd_config && sudo systemctl restart ssh
>    # 云服务器安全组放行你选的端口，例如 10080
>    ```
>    
>    ```bash
>    # Ubuntu 本地侧：把本地 80 推到云服务器的 10080
>    ssh -N -R 10080:localhost:80 root@<云服务器公网IP>
>    # 然后在任何地方访问 http://<云服务器公网IP>:10080 就是访问你本地的服务
>    ```
> 
> 2. **组虚拟局域网**：Tailscale / ZeroTier，两边装上就互通，不用管 NAT。优点是省心，缺点是引入第三方。
> 
> 3. **内网穿透（frp / ngrok）**：企业里常见，但配置比前两个重，第 5 月再碰。
> 
> **如果嫌麻烦**：局域网内用**第二台设备**（手机热点 + 笔记本、或另一台电脑）连你的 Ubuntu 来做"跨机验证"，也算达成目标，只是少了公网这一层。

#### ⑥ 文件交换方案（统一到一处，别两头放）

| 内容             | 放哪                  | 怎么跨系统访问                                          |
| -------------- | ------------------- | ------------------------------------------------ |
| **代码、脚本、实验记录** | Ubuntu `~/projects` | **用 Git 同步**（推到 GitHub/Gitee），Windows 那边克隆一份只读参考 |
| 笔记、架构图、文档      | Windows             | 在 Ubuntu 里挂载读取（前提是做了第 ① 步）                       |
| 临时文件           | 云盘 / U 盘            | 少量使用即可                                           |

**铁律：代码只存 Ubuntu 一份，走 Git 同步。** 两边各放一份，一周后你就分不清哪个是新的了。

**在 Ubuntu 里挂载 Windows 分区**（做完第 ① 步后可用）：

```bash
lsblk -f                                   # 找到 Windows 分区
sudo mkdir -p /mnt/win_d
sudo mount -t ntfs3 /dev/nvme0n1pX /mnt/win_d -o uid=$(id -u),gid=$(id -g)
# 如果 ntfs3 报错或写法异常，装用户态驱动回退：
# sudo apt install -y ntfs-3g && sudo mount -t ntfs-3g ...
```

> **仅用于读写笔记文档。不要把代码/Docker 项目放这里** —— NTFS 没有 Linux 权限模型，Docker 构建会报奇怪的权限错误。
> 从 Linux 往 NTFS **写入**始终有元数据风险，重要文件在 Windows 侧再留一份。

#### ⑦ 建立"任务块"节奏（双系统最重要的工作方式）

重启一次 1~2 分钟，来回切换一天浪费 5~10 分钟。**更贵的是打断心流**——切过去刷个视频就回不来了。

| 时间                  | 系统                         | 只做这些事                                      |
| ------------------- | -------------------------- | ------------------------------------------ |
| 工作日 20:00-22:00（2h） | **Ubuntu**                 | 敲命令、做实验、写脚本。**笔记直接写在 Ubuntu 里**，不回 Windows |
| 周末上午                | **Ubuntu**                 | 大段实验、本周交付物                                 |
| 周末下午                | **Ubuntu**（可切 Windows 查资料） | 项目推进                                       |
| 周末傍晚 30 min         | **Windows**                | 集中写周复盘、画架构图、整理成文档                          |
| 第 5-6 月需要开云控制台时     | **Windows**                | 控制台 + Terraform 代码在 Ubuntu，两边配合            |

**核心原则：工作日 2 小时只在 Ubuntu 里，不切系统。** 需要查资料时用 Ubuntu 的浏览器或命令行工具（`curl` 官方文档、`man`）。

#### 双系统常见故障速查

| 现象                    | 原因                         | 解决                                                           |
| --------------------- | -------------------------- | ------------------------------------------------------------ |
| Ubuntu 里 Windows 分区只读 | 快速启动/休眠没关                  | 执行上面的 `powercfg /h off`                                      |
| 时间差 8 小时              | RTC 模式不一致                  | Windows 侧 `RealTimeIsUniversal=1`，Ubuntu 侧 `set-local-rtc 0` |
| 开机直接进 Windows，没有选择菜单  | Windows 更新覆盖了引导            | 用 Ubuntu Live USB + `boot-repair` 修复                         |
| 100G 分区满了扩不了          | 分区后面还有别的分区                 | 用"新分区挂到 `/var/lib/docker`"的方案绕过                              |
| 挂载新分区后 Docker 起不来     | 挂载盖住了旧数据                   | 停 Docker → rsync 迁移 → 再挂载（见 1.5④）                            |
| 重启后进 emergency mode   | `/etc/fstab` 写错（UUID 或挂载点） | 输入 root 密码进维护模式，`vim /etc/fstab` 注释掉新行后重启                    |
| 改分区后 Windows 报错       | NTFS 元数据被改动                | Windows 里跑 `chkdsk /f`                                       |

### 1.5-A 虚拟机分支（如果你是虚拟机，看这一节）

虚拟机不用折腾 1.5 的任何一条，而且有 4 个双系统拿不到的优势，**要用足**：

1. **快照 = 后悔药。** 每开始一个破坏性练习（W2 改 sudoers、W7 改防火墙、W15 集群玩坏）**之前先打快照**，搞坏了直接回滚。这是虚拟机最大的价值，没有之一。
2. **可以和 Windows 同时开。** 视频、文档、控制台都在 Windows，实验在虚拟机，不用重启。时间块设计可以简化：工作日 2h 全在虚拟机里，Windows 只在周末。
3. **网络模式按需切**：
   - 只在本机验证 → NAT 就够；
   - 要让**宿主机**能访问（浏览器打开虚拟机里的服务）→ 用 NAT + 端口转发，或直接桥接；
   - 要让**云服务器/其他设备**访问 → 必须桥接。
4. **分配要合理。** 24G 内存里给虚拟机 **12~16G**、CPU 给 4~6 核、磁盘给 100G 且**选"拆分/按需增长"以外的固定分配**（动态磁盘在高 IO 场景会拖慢）。**不要关掉图形界面**——虚拟机里纯文本模式的收益不大，反而少了浏览器查文档的路径。

**虚拟机特有的两个坑**：

- **时间漂移**：长时间挂起/恢复后容器和 K8s 证书时间会错，装 `open-vm-tools` 或启用 guest additions 的时间同步。
- **嵌套虚拟化**：k3d 需要 Docker，Docker 在部分虚拟化环境下要开"虚拟化引擎/VT-x 直通"，否则起不来。装之前先在设置里打开。

### 1.6 阿里云账号准备（第 5 月才用，但建议提前注册观察）

| 项目        | 说明                                                                        |
| --------- | ------------------------------------------------------------------------- |
| 账号        | 个人实名认证，支付宝可直接认证                                                           |
| **费用预警**  | **注册后第一件事：设置"可用额度预警"和"预算"，阈值设 50 元**                                      |
| RAM 子账号   | 不要用主账号 AccessKey。创建子账号 + 最小权限策略                                           |
| AccessKey | **绝不写进代码、绝不提交 Git**。用环境变量或 `~/.aliyun/config.json`（注意：该文件是**明文**，权限设 600） |
| 安全组       | 默认只开 22 和 80/443，**不要开 0.0.0.0/0 的 3306 和 6379**                          |
| OpenAPI   | 第 6 月 Terraform 要用，提前在控制台生成 AccessKey 并测试 `aliyun` CLI                    |
| 域名        | 首年 30~60 元，W19 买。**域名实名认证通过后通常还要等 3 天才能提交备案**                             |
| 备案        | **大陆节点 + 域名对外访问必须备案，7~20 个工作日**。不想等就用中国香港节点（免备案）                          |

> **一个会烧钱也会卡死计划的坑：轻量应用服务器走不通。** 很多教程会建议"轻量更便宜"，但在你这个计划里它做不到下面这些事，官方限制如下：
> 
> | 能力                    | 轻量应用服务器                                                       | 标准 ECS          |
> | --------------------- | ------------------------------------------------------------- | --------------- |
> | 加入自定义 VPC / 自己划交换机和子网 | **不支持**（自动创建、不可改）                                             | 支持              |
> | 挂标准 SLB 做四层/七层负载均衡    | **不支持**（只有独立的"轻量负载均衡"，60 元/月、**仅七层 HTTP**、仅北京/杭州/深圳、每地域限 1 个） | 支持              |
> | NAT 网关 / EIP / 网络 ACL | **不支持**                                                       | 支持              |
> | 计费方式                  | **仅包年包月**（无法"用完即释放"）                                          | 包年包月 / 按量 / 抢占式 |
> | 支持 IPv6 / 多块数据盘       | 不支持 / 仅 1 块                                                   | 支持              |
> 
> 而你的 W18 要"规划 3 子网 VPC"、W19 要"SLB 挂两台 ECS"、W21 要"多可用区无单点"、W23 要"Terraform 用代码建 VPC+安全组+SLB"——**这四条轻量服务器一条都做不到**。
> 
> **结论：W18-W21 全部用标准 ECS，按量付费，实验做完立刻释放。** 轻量应用服务器只适合最后放个静态博客/个人站，不当学习主力。
> 
> 另外轻量服务器还有个坑：**它的"备案"要求套餐购买时长 ≥3 个月**，你要是买月付，连备案门槛都进不去。

### 1.7 环境验收标准

**在 Ubuntu 里**跑通下面全部 8 条，跑不通不许往下学：

| #   | 验收项           | 通过标准                                                                                                                                     |
| --- | ------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | 系统确认          | `cat /etc/os-release` 显示 26.04；`uname -r` 记下内核版本                                                                                         |
| 2   | 资源确认          | `nproc` / `free -h` / `df -h /` 三项数字都记下来（内存、剩余磁盘）                                                                                        |
| 3   | Docker 免 sudo | **重新登录后** `docker ps` 不报 permission denied                                                                                               |
| 4   | 容器跑通          | `docker run --rm hello-world` 成功输出                                                                                                       |
| 5   | 端口验证          | `docker run -d -p 8080:80 nginx` 后，**在 Ubuntu 里 `curl -I localhost:8080` 返回 200**                                                        |
| 6   | K8s 跑通        | `k3d cluster create demo` → `kubectl get nodes` 显示 1 个 Ready                                                                             |
| 7   | SSH 可用        | `systemctl status ssh` 正常，`ssh localhost` 能登录                                                                                            |
| 8   | **形态专项**      | **双系统**：快速启动已关、`timedatectl` 显示 `RTC in local TZ: no`、`lsblk -f` 看清分区结构、能挂载 Windows 分区。**虚拟机**：快照功能可用（试打一次回滚）、桥接/NAT 端口转发按需配好、分配内存 ≥12G。 |
| 9   | **备份可用**      | 已建立回滚手段（见 1.8），并**实际演练过一次恢复**                                                                                                            |

> 第 8、9 条必须现在做。**现在不做，第 3 月写脚本要用 Windows 文件、第 4 月 Docker 爆盘、第 2 月改坏 sudoers 时都会卡住。**

### 1.8 备份与回滚

**为什么必须有**：这份计划里有至少 5 处"故意搞坏"的练习（W2 改坏 sudoers、W7 改防火墙、W15-17 玩坏集群、W25 宕机演练）。**没有回滚手段的破坏性练习不叫练习，叫事故。**

| 形态  | 方案                      | 说明                                                                                       |
| --- | ----------------------- | ---------------------------------------------------------------------------------------- |
| 虚拟机 | **快照**（首选）              | 每个破坏性练习前打一次，命名写清日期和内容                                                                    |
| 双系统 | **Timeshift**（RSYNC 模式） | `sudo apt install timeshift`，首次全量、之后增量。**注意要吃额外磁盘空间，100G 盘上先估算**，必要时把备份写到 Windows 分区上的目录 |
| 双系统 | 配置备份（轻量兜底）              | `sudo tar czf /mnt/win_d/etc-backup-$(date +%F).tgz /etc` —— 改了 `/etc` 下任何东西之前先来一发       |
| 通用  | **Git 管住你的代码**          | `~/projects` 全部进 Git，推到远端。代码丢了比环境坏了更痛                                                    |

**双系统改坏 sudoers 之后的救援路径（提前知道，别临场搜）**：

1. 开机在 GRUB 菜单按 `e` 编辑内核行，在 `linux` 那行末尾加 `init=/bin/bash`；
2. `mount -o remount,rw /` 把根分区改成可写；
3. `visudo` 修好，或者 `cp /etc/sudoers.bak /etc/sudoers`；
4. `exec /sbin/init` 或直接重启。

**所以 W2 D7 之前，请先 `sudo cp /etc/sudoers /etc/sudoers.bak` 并保证这个备份在另一个分区上也有一份。**

### 1.9 目录与笔记规范

* 代码/脚本：`~/projects/<模块名>`（如 `~/projects/linux-lab`、`~/projects/k8s-demo`）
* 笔记：Ubuntu 下 `~/notes/`，周末同步一版到 Windows 的 Obsidian Vault
* 报错手册：`~/notes/errors.md`，**每条格式固定**：现象 / 命令 / 报错原文 / 根因 / 解法 / 一句话总结
* 每个交付物一个 Git 仓库，`README.md` 写清"这是什么、怎么跑、产出长什么样"

---

## 第二部分：时间与节奏设计

### 2.1 每日时间块（工作日 2h）

| 时段    | 时长     | 内容                           | 铁律                   |
| ----- | ------ | ---------------------------- | -------------------- |
| 复盘    | 10 min | 用 Anki 或笔记本回顾前一天，**先合上笔记默写** | 不复盘 = 白学             |
| 输入+动手 | 70 min | 看教程 ≤ 25 min，剩下 45 min 必须自己敲 | **看视频时间不得超过总时间 1/3** |
| 收尾    | 40 min | 记笔记：今天学了什么、报错是什么、怎么解的        | 必须留痕                 |

**最重要的一条规则：输入时长 ≤ 输出的 1/2。** 看教程看爽了但没敲，是最隐蔽的假努力。

> **口径提醒**：这一小时 50 分钟是**复盘和笔记**，只有 70 分钟在学新东西。所以"工作日 2h × 26 周"听起来很多，**真正长本事的时长要打折**——这就是为什么 2.2 的周末交付物不能省。

### 2.2 周末时间块（4~6h）

| 时段  | 时长     | 内容                     |
| --- | ------ | ---------------------- |
| 上午  | 2~2.5h | 把本周零散知识点串成一条线，做一件完整的小事 |
| 下午  | 2~2.5h | **本周交付物**（见各周"周末交付"）   |
| 收尾  | 30 min | 写周复盘，填检查点表，规划下周        |

### 2.3 每周节奏

- **周一到周四**：输入新知识 + 跟做
- **周五**：本周"周检查点"自测（不看资料能不能做出来）
- **周六**：整合练习 + 交付物
- **周日**：交付物收尾 + 复盘 + **主动休息半天**（不是偷懒，是防止第 8 周崩盘）

### 2.4 参考日模板（工作日通用）

**A 型日（输入日）**

| 时间          | 动作                | 当天产出   |
| ----------- | ----------------- | ------ |
| 0-10 min    | 默写昨日 3 个知识点       | 默写记录   |
| 10-35 min   | 看新知识点教程           | 理解即可   |
| 35-110 min  | 自己敲一遍 + 故意改坏再看怎么修 | 可复现的操作 |
| 110-120 min | 记录笔记 + 报错日志       | 笔记 1 条 |

**B 型日（练习日）**

| 时间          | 动作                        | 当天产出   |
| ----------- | ------------------------- | ------ |
| 0-10 min    | 默写昨日                      | 默写记录   |
| 10-100 min  | 只做练习，不看教程。卡住先自己查 man/官方文档 | 练习结果截图 |
| 100-120 min | 把卡点写清楚，标注"未解决"            | 问题清单   |

**关键：B 型日不许看教程。** 逼自己查 `man` 和官方文档，这是工程师和培训学员的分水岭。

---

## 第三部分：6 个月总览

| 月     | 周次      | 主题                              | 月末必须拿出的东西                               |
| ----- | ------- | ------------------------------- | --------------------------------------- |
| 第 1 月 | W1-W4   | Linux 系统与排障                     | 一台能自己修好的 Ubuntu + 排障反射                  |
| 第 2 月 | W5-W8   | 网络原理与抓包（+ **Nginx**）            | 一份自己抓的 TCP/HTTP 抓包分析报告 + 一个跑通的 Nginx 反代 |
| 第 3 月 | W9-W12  | Shell + Python 自动化              | 批量 SSH 巡检脚本（真实可用）                       |
| 第 4 月 | W13-W17 | Docker + Kubernetes             | 自己的应用跑在 K8s 上，能滚动更新回滚                   |
| 第 5 月 | W18-W21 | 阿里云吃透（+ **Prometheus/Grafana**） | 一套真实付费搭出的高可用架构 + 架构图 + 本地监控栈            |
| 第 6 月 | W22-W26 | CI/CD + Terraform + 毕业项目        | 端到端可复现项目 + 简历 + 开始投递                    |

**26 周共 3 个"生死线"**，过不去就必须停下补，不要硬冲：

- **W4 结束**：能不能不看教程排查一个服务起不来的问题
- **W12 结束**：能不能写出 100 行以上、真的能跑的自动化脚本
- **W17 结束**：能不能把一个应用完整部署到 K8s 并排掉一个故障

> **提醒：第 5 月负载明显超标。** 4 周要装下 ECS + VPC + SLB + NAT + EIP + DNS + 证书 + RDS + OSS + CDN + 云监控 + 快照 + 压测 + 成本核算共 14 个主题，而第 4 月给"仅"Docker + K8s 就用了 5 周。**按 6.1 的优先级执行**：ECS / VPC / 安全组 / SLB / 云监控告警是 P0，RDS / OSS / CDN 只要求"跑通一次并知道什么场景用"。

---

## 第四部分：逐月逐周逐日执行表

> **说明**：工作日按 A 型日 / B 型日交替，内容已逐日写死。周末交付物单独列出。
> 每天的"达成结果"就是那一格的产出，做到了就打勾。

---

## 第 1 月：Linux 系统与排障

**月度目标**：告别"会敲命令"，建立"看症状→定位→验证"的排障反射。
**月度交付**：① 一台被自己改坏又修好的 Ubuntu；② 一份 20 条以上的报错-原因-解法笔记。

### W1：环境搭建 + Linux 入门

**周目标**：环境全部跑通，能在 Ubuntu 里自由操作文件系统。

| 日   | 类型  | 内容                                                                       | 当天达成结果                                   |
| --- | --- | ------------------------------------------------------------------------ | ---------------------------------------- |
| D1  | 搭建  | 按 1.1 装完 Ubuntu 全部工具 + 开启 sysstat + 确认系统/内存/磁盘                           | 环境验收前 2 条通过                              |
| D2  | 搭建  | 按 1.2 配 Docker + 镜像加速 + 日志轮转 + SSH；按 1.5（或 1.5-A）做完形态设置；**建好 1.8 的回滚手段** | **环境验收 9 条全部通过**                         |
| D3  | A   | Linux 目录结构、`cd/ls/pwd/mkdir/cp/mv/rm`                                    | 能不查资料说出 `/etc /var /usr /opt /home` 各放什么 |
| D4  | B   | 纯练习：在 `~/projects/lab` 下建目录树并操作                                          | 手不碰鼠标完成 20 个文件操作                         |
| D5  | A   | 文件查看与搜索：`cat/less/head/tail/find/grep`                                   | 能用一行命令找出某目录下 7 天内改过的 .log                |
| D6  | 周末  | 熟悉 `vim` 基础（i / Esc / :wq / dd / yy / p）                                 | 能用 vim 完整编辑一个文件不退出重来                     |
| D7  | 周末  | 周复盘 + 交付：写"环境搭建笔记"                                                       | 一篇含 5 个坑的搭建记录                            |

**W1 检查点**：`docker run --rm hello-world` 通过 + `curl -I localhost:8080` 返回 200 + 形态设置全部做完 + **快照/备份可用** + 能在 Ubuntu 内 vim 编辑保存文件。

### W2：权限、用户、软件包

**周目标**：彻底搞懂 Linux 权限模型和包管理，这是后面所有故障的基础。

| 日   | 类型  | 内容                                            | 当天达成结果                 |
| --- | --- | --------------------------------------------- | ---------------------- |
| D1  | A   | 权限 `rwx`、`chmod`、`chown`、数字表示法                | 能解释 755 和 644 的每一位含义   |
| D2  | B   | 练习：造 6 个不同权限的文件，逐个验证                          | 能预测每个文件的读写执行结果并验证正确    |
| D3  | A   | umask、特殊权限 SUID/SGID/Sticky                   | 能说出 `/tmp` 为什么是 1777   |
| D4  | B   | 用户与组：`useradd/usermod/groupadd/id/su/sudo`    | 新建用户并让它在指定目录有写权限       |
| D5  | A   | 包管理 `apt`、软件源、`dpkg`                          | 能自己换软件源并装一个包           |
| D6  | 周末  | `/etc/passwd` `/etc/shadow` `/etc/sudoers` 精读 | 能说出用户密码存在哪、为什么         |
| D7  | 周末  | 周复盘 + 交付：**故意把 sudo 配错并修回来**                  | 一篇"我把 sudo 搞坏了又救回来"的记录 |

**W2 检查点**：能解释"一个用户能进目录但读不了文件"的原因。

> **⚠️ W2 D7 是整份计划里最危险的一天，安全网必须提前就位**
> Ubuntu **默认 root 账号没有密码**。如果你用非 `visudo` 的方式改坏 `/etc/sudoers`，同时又关掉了当前 root 会话，你就**彻底失去提权通道**，只能走 1.8 的 GRUB `init=/bin/bash` 救援或重装。
> **开工前三条**：
> 
> 1. `sudo cp /etc/sudoers /etc/sudoers.bak`（并确保这个备份在另一个分区也有一份）；
> 2. **必须用 `visudo`**——它会做语法检查，写错会拦住你并让你重来，`vim /etc/sudoers` 不会；
> 3. 动手前先 `sudo -s` 开一个**保持不关的 root shell**（在另一个 tmux 窗口里），就算改坏了也能从这个窗口救回来。
>    虚拟机用户：先打快照，然后随便折腾。这才是这套练习的正确打开方式。

### W3：进程、内存、磁盘、systemd

**周目标**：这是全月最核心的一周。掌握"资源类故障"的排查链路。

| 日   | 类型  | 内容                                           | 当天达成结果                   |
| --- | --- | -------------------------------------------- | ------------------------ |
| D1  | A   | 进程基础 `ps/top/htop`、进程状态、父子进程                 | 能看懂 `ps aux` 每一列         |
| D2  | B   | 信号与前后台 `kill -9/-15`、`nohup`、`&`、`jobs`      | 能说出 -9 和 -15 的区别及危害      |
| D3  | A   | 内存：`free/vmstat`、buffer/cache、OOM Killer     | 能解释为什么 free 很少但系统不卡      |
| D4  | B   | 磁盘：`df/du/lsblk/lsof`，inode，挂载               | 能定位"磁盘满"是空间问题还是 inode 问题 |
| D5  | A   | **systemd**：unit 文件、`systemctl`、`journalctl` | 能独立写一个 unit 托管自己的脚本并开机自启 |
| D6  | 周末  | 排障实战：`stress-ng` 压 CPU/内存，边压边排查              | 一份三个场景的排障记录（CPU/内存/磁盘）   |
| D7  | 周末  | 周复盘 + 交付：**自制一个监控脚本由 systemd 托管**            | 脚本每 5 分钟记录一次负载到文件        |

> 用 `stress-ng` 压内存时，**别让它把系统本身压死**（OOM Killer 会先杀你的桌面或 sshd）。给它套一个内存上限：
> 
> ```bash
> sudo systemctl-run --scope -p MemoryMax=4G stress-ng --vm 2 --vm-bytes 3G --timeout 60s
> ```
> 
> 顺便这一下就把 cgroup 的资源限制讲明白了，比只看文档强。

**W3 检查点（月度生死线预演）**：给你一个起不来的服务，你能用 `systemctl status` → `journalctl -u` → 看端口 → 看配置的顺序定位到原因。

### W4：日志、定时任务、SSH、综合评价

**周目标**：补完 Linux 运维的最后几块拼图，做月度考核。

| 日   | 类型  | 内容                                                    | 当天达成结果                                       |
| --- | --- | ----------------------------------------------------- | -------------------------------------------- |
| D1  | A   | 日志体系：`/var/log`、`rsyslog`、`logrotate`                 | 能说出日志为什么会被自动切割                               |
| D2  | B   | `journalctl` 高级用法：按时间/服务/优先级过滤                        | 能一条命令查出昨天某服务的所有 error                        |
| D3  | A   | **定时任务：`crontab` 语法 + 坑，以及 `systemd timer`**          | 一个每天 3 点备份的任务 + 一个 `Persistent=true` 的 timer |
| D4  | B   | SSH 深入：密钥认证、`config` 文件、`scp/rsync`、端口转发（含 `-R` 反向隧道） | 配好免密登录 + 本地转发与反向隧道各跑通一次                      |
| D5  | A   | 综合：把前 4 周串起来                                          | 画出自己的"Linux 排障决策树"                           |
| D6  | 周末  | **月度考核**：限时 2 小时完成 5 个故障场景                            | 5 题至少做出 4 题                                  |
| D7  | 周末  | 月复盘 + 整理"报错手册 v1"                                     | ≥20 条报错记录，含原因和解法                             |

> **关于 D3 和 D4 的两点补充：**
> 
> - **别再只学 cron。** 2026 年的生产环境里 `systemd timer` 已经很常用，而且它解决了 cron 最大的痛点：机器当时没开机就永远不跑。用 `Persistent=true` 补跑，这才是正解。另外记住 cron 的两个排查点：日志在 `journalctl -u cron`（或 `/var/log/syslog`）里；`PATH` 和 `MAILTO` 是最常见的"手工能跑、定时不跑"原因。
> - **D4 加上 `ssh -R`（反向隧道）**。第 5 月的跨机验证全指望它，而且它是面试高频题（"内网机器怎么让外面访问到"）。

**月度考核 5 题（自己出题或找题）**

1. 服务起不来，端口被占用
2. 磁盘满，但 `du` 找不到大文件（被删未释放）
3. 用户登录不上，报 permission denied
4. 定时任务没执行
5. 内存被 OOM Killer 杀掉进程，找出是谁

---

## 第 2 月：网络原理与抓包

**月度目标**：能独立解释"为什么连不上"，从 ping 到 curl 一条链走完。
**月度交付**：一份真实的 TCP 握手 + HTTP 请求抓包分析报告；一个跑通的 Nginx 反向代理。

### W5：TCP/IP 与分层模型

| 日   | 类型  | 内容                                    | 当天达成结果                |
| --- | --- | ------------------------------------- | --------------------- |
| D1  | A   | 网络分层、OSI vs TCP/IP、封装解封装              | 能画出数据从应用到网线的完整封装过程    |
| D2  | B   | IP 地址、子网掩码、CIDR 计算                    | 能手算子网划分，不看工具          |
| D3  | A   | TCP 三次握手、四次挥手、状态机                     | 能默写状态转换图并解释 TIME_WAIT |
| D4  | B   | 端口与套接字 `ss -lntup`、`netstat`          | 能查清一个端口被谁占用           |
| D5  | A   | UDP vs TCP、ICMP、`ping/traceroute/mtr` | 能解释 ping 通但端口连不上的原因   |
| D6  | 周末  | Wireshark 入门：抓一次 `curl` 请求            | 能在抓包文件里找到三次握手         |
| D7  | 周末  | 周复盘 + 交付：**画出三次握手抓包标注图**              | 一张标注了 SYN/ACK/seq 的截图 |

> D4 用 `ss -lntup`，并记住**非 root 时 `-p` 看不到别人进程的端口**。查"这个端口到底被谁占了"要用：
> 
> ```bash
> sudo ss -lntup | grep :8080
> sudo lsof -i :8080        # 备选，输出更直白
> ```
> 
> D6 抓包要选对网卡，这是新手第一个卡点：抓**本机访问外部**的流量用默认网卡（`tcpdump -i any -nn`），抓**本机 loopback 服务**（比如 localhost:8080）必须用 `-i lo`。抓不到自己的流量，八成是网卡选错了。

### W6：DNS 与 HTTP/HTTPS

| 日   | 类型  | 内容                                 | 当天达成结果                     |
| --- | --- | ---------------------------------- | -------------------------- |
| D1  | A   | DNS 原理：递归/迭代、各级缓存、记录类型             | 能说出 A/CNAME/MX/NS/TXT 用途   |
| D2  | B   | `dig`/`nslookup`/`hosts` 实战        | 能用 `dig +trace` 走完一次完整解析   |
| D3  | A   | DNS 故障排查：解析不了的 8 种原因               | 能按顺序排查"域名打不开"              |
| D4  | A   | HTTP 协议：方法、状态码、Header、Cookie       | 能解释 301/302/304/502/504 区别 |
| D5  | B   | `curl` 实战：`-v` `-I` `-x` `-H` `-d` | 能用一个 curl 命令定位是 DNS 还是服务问题 |
| D6  | 周末  | HTTPS：证书链、握手过程、`openssl s_client`  | 能检查一个网站的证书有效期和签发链          |
| D7  | 周末  | 周复盘 + 交付：**HTTP 抓包分析报告（含 502 案例）** | 报告含请求响应全流程                 |

### W6.5 Nginx 必学（0.5~1 天，不占整周）

> **为什么单独立一课**：Nginx 是运维岗面试第一高频题（"502 和 504 怎么查""反代怎么配 TLS"），而且它是后面很多内容的底座——日志分析、K8s 入口、HTTPS 自动续期都要用到它。
> 
> 用一天做完这 5 件事，全部本地可跑、零成本：

```bash
sudo apt install -y nginx
```

1. **反向代理**：起一个本地服务（先 `python3 -m http.server 8000`），写一个 `location /` 转发过去，验证 `curl -I localhost` 通；
2. **虚拟主机 + 域名**：配两个 `server` 块，用 `/etc/hosts` 做 `a.local` / `b.local`，验证按 Host 分流；
3. **HTTPS**：用自签证书跑通 443（`openssl req -x509 ...`），理解证书链；
4. **负载均衡**：`upstream` 挂两个后端，`curl` 多次看轮询；**停掉一个后端，观察 Nginx 返回什么**（这是 502 的现场）；
5. **亲手造一次 502 和一次 504**：
   - 502 = 后端连不上/立刻拒绝 → 把后端停掉；
   - 504 = 后端连上了但超时 → 后端里塞个 `sleep 60`；
   - 对照 `error.log` 里的原文，把这两条写进报错手册。

**交付**：一个能跑反代 + HTTPS + 负载均衡的 Nginx 配置，加一份 `error.log` 里 502/504 的原样报错记录。

> 顺带：这也是第 10 周那份"真实 access.log"的来源——**用 Nginx 自己产生**，不用去网上找。

### W7：路由、NAT、防火墙

| 日   | 类型  | 内容                                                           | 当天达成结果                                |
| --- | --- | ------------------------------------------------------------ | ------------------------------------- |
| D1  | A   | 路由表、网关、`ip route`、静态路由                                       | 能读懂本机路由表每一条                           |
| D2  | B   | NAT 原理、SNAT/DNAT、端口映射                                        | 能解释家里路由器怎么让内网访问外网                     |
| D3  | A   | **iptables 四表五链 + nftables 与规则持久化**                          | 能写出放行 80、拒绝某 IP 的规则，且重启后仍生效           |
| D4  | B   | `ufw`（Debian 系）/ `firewalld`（RHEL 系）实战 + **Docker 绕过防火墙的实验** | 能配好一台机器只开 22/80，并解释 Docker 为什么会绕过 ufw |
| D5  | A   | 云上网络概念预告：VPC、安全组、SLB                                         | 能说出安全组和 iptables 的区别                  |
| D6  | 周末  | 抓包实战：抓一次"连不上"的完整过程                                           | 能从抓包判断是拒绝还是丢包                         |
| D7  | 周末  | 周复盘 + 交付：**网络排障决策树 v1**                                      | 一张从物理层到应用层的排查图                        |

> **这一周有两个必考考点，别漏：**
> 
> **① iptables 规则默认重启即失**。用手写的规则重启后就没了，工程上必须落地：
> 
> - Debian 系：`sudo apt install iptables-persistent`，`netfilter-persistent save`
> - Ubuntu 默认已经是 nftables 后端，也可以直接写 `/etc/nftables.conf` 并用 `systemctl enable --now nftables`
> - **"我明明加了规则，重启就失效"**是真实故障，别以为自己手滑。
> 
> **② Docker 会直接改写 iptables，绕过 ufw —— 这是最经典的生产事故现场。**
> 现象：你在 ufw 里 deny 了 3306，但 `docker run -p 3306:3306 mysql` 之后，**这个端口对全世界开放**。
> 原因：Docker 把自己的规则插进 `DOCKER` 链，走的是 `nat/PREROUTING → FORWARD`，**根本不经过 ufw 管的 `INPUT` 链**。
> 动手复现一次（ufw deny 一个端口 → 用 `-p` 发布 → 从另一台设备访问），然后记住三种解法：
> a) 发布时绑定回环：`-p 127.0.0.1:3306:3306`（**最常用、最省心**）；
> b) 改 `daemon.json` 里的 `"iptables": false` 并自己管规则（麻烦，不推荐新手）；
> c) 用 `DOCKER-USER` 链加限制规则。
> **这个点在面试里几乎必问**："你遇到过端口明明防火墙拦了还是能访问吗？"

### W8：网络综合与月度考核

| 日   | 类型  | 内容                                      | 当天达成结果           |
| --- | --- | --------------------------------------- | ---------------- |
| D1  | A   | 综合：域名到页面的完整链路（含 CDN 概念）                 | 能画出并讲清这 8 步      |
| D2  | B   | `tcpdump` 实战：按端口/主机/协议过滤                | 能抓出指定流量并导出 pcap  |
| D3  | A   | 常见故障案例库：超时/拒绝/RST/DNS 污染                | 每个案例能说出定位第一步     |
| D4  | B   | 用 `mtr` + `tcpdump` 定位一次慢请求             | 能区分是网络慢还是服务慢     |
| D5  | A   | 网络知识查漏补缺 + 错题回顾（含 W6.5 的 Nginx 502/504） | 无未掌握的高频概念        |
| D6  | 周末  | **月度考核**：给 5 个"打不开"场景，逐个定位              | 5 题做出 4 题，写出定位过程 |
| D7  | 周末  | 月复盘 + 整理"网络排障手册 v1"                     | 手册含 10 个真实案例     |

---

## 第 3 月：Shell + Python 自动化

**月度目标**：把重复劳动变成脚本。**这是"运维"和"运维工程师"的分界线。**
**月度交付**：一个批量 SSH 巡检脚本（真实可用、有日志、有异常处理）。

> **⚠️ 动手前先解决"多台机器从哪来"的问题（不解决这里一定会卡住）**
> W12 要求"并发连多台主机收 CPU/内存/磁盘"，但云服务器要到第 5 月才买。**第 3 月你必须自己造靶机。**
> 好消息是 W1 就装好了 Docker，所以零成本方案现成的——**用容器造 5 台"假服务器"**：

```bash
# 起 5 个带 sshd 的容器，分别映射到 2221~2225（避免和宿主 22 冲突）
for i in 1 2 3 4 5; do
  docker run -d --name node$i \
    -p 222$i:22 \
    --memory=512m --cpus=0.5 \
    linuxserver/openssh-server:latest \
    -e PUID=1000 -e PGID=1000 -e USER_NAME=ops -e USER_PASSWORD=test1234 2>/dev/null || \
  docker run -d --name node$i -p 222$i:22 --memory=512m --cpus=0.5 \
    rastasheep/ubuntu-sshd:18.04
done
docker ps --format '{{.Names}}\t{{.Ports}}'
```

> 这样你就有 5 台"能 ssh、能被采集、能被搞坏"的靶机，而且可以随时 `docker rm -f` 重造——**比云服务器更适合练批量运维**（云服务器删了要重买）。
> **额外收益**：`--memory=512m` 的容器很适合做"OOM Killer 现场"和"磁盘满"的演练，不用污染宿主。
> 注意：容器里的 `df`、`free` 反映的是容器视角（部分指标来自宿主 cgroup），采集脚本里要能解释这一点，别在面试时被问倒。

### W9：Bash 基础

| 日   | 类型  | 内容                                           | 当天达成结果                       |
| --- | --- | -------------------------------------------- | ---------------------------- |
| D1  | A   | 变量、引号区别、`$()`、环境变量、`export`                  | 能说清单引号双引号反引号区别               |
| D2  | B   | 条件判断 `if/test/[[ ]]`、文件测试符                   | 写出判断文件/目录/权限的脚本              |
| D3  | A   | 循环 `for/while/until`、`case`、`continue/break` | 写出遍历目录批量改名的脚本                |
| D4  | B   | 函数、参数 `$1/$@/$#`、返回值、`set -euo pipefail`     | 写出带参数校验的函数库                  |
| D5  | A   | 管道与重定向 `>/>>/2>&1/tee/xargs`                 | 能解释 `2>&1` 为什么不能写成 `2>&1 1>` |
| D6  | 周末  | `ShellCheck` 用法 + 改掉自己脚本的坏习惯                 | 脚本 ShellCheck 无 error        |
| D7  | 周末  | 周复盘 + 交付：**磁盘巡检脚本（单机版）**                     | 超阈值输出告警并写日志                  |

### W10：文本处理与实战脚本

| 日   | 类型  | 内容                                    | 当天达成结果                   |
| --- | --- | ------------------------------------- | ------------------------ |
| D1  | A   | `grep` 正则、`-E` `-o` `-v` `-r` `-A/-B` | 能从日志里精确捞出想要的字段           |
| D2  | B   | `sed` 替换/删除/按行操作                      | 能批量改配置文件的某个值             |
| D3  | A   | `awk` 字段处理、条件、统计求和                    | 能用 awk 统计日志里每个 IP 的请求数   |
| D4  | B   | `cut/sort/uniq/wc/tr/join` 组合拳        | 能从 nginx 日志出 Top10 访问 IP |
| D5  | A   | 日志分析实战：分析一份真实 access.log              | 输出状态码分布、Top URL、Top IP   |
| D6  | 周末  | 脚本健壮性：`trap`、临时文件、并发控制                | 脚本被 Ctrl+C 不残留垃圾文件       |
| D7  | 周末  | 周复盘 + 交付：**Nginx 日志分析脚本**             | 一条命令输出 5 项统计报表           |

> D5 说的"一份真实 access.log"，**来源就是 W6.5 里你自己起的 Nginx**。造流量不需要等真实用户：
> 
> ```bash
> # 用 ab 或 wrk 打一轮，顺便把状态码分布和 Top IP 造得更真实
> sudo apt install -y apache2-utils
> ab -n 2000 -c 20 http://localhost/
> # 想造出 404/502 的多样性，用 curl 脚本反复打不同路径，中途停一次后端
> ```

### W11：Python 基础（够用为止）

| 日   | 类型  | 内容                              | 当天达成结果          |
| --- | --- | ------------------------------- | --------------- |
| D1  | A   | 环境、变量、类型、字符串、f-string           | 能写出格式化输出的脚本     |
| D2  | B   | 列表/字典/集合/元组 + 推导式               | 能处理嵌套字典数据       |
| D3  | A   | 流程控制、函数、默认参数、`*args`            | 写出带默认参数的函数      |
| D4  | B   | 文件读写、`with`、json/csv 处理         | 能读写 JSON 配置文件   |
| D5  | A   | 异常处理 `try/except/finally`、自定义异常 | 脚本报错不崩，有友好提示    |
| D6  | 周末  | 模块与包、`argparse` 命令行参数           | 脚本支持 `-h` 和参数传入 |
| D7  | 周末  | 周复盘 + 交付：**用 Python 重写磁盘巡检脚本**  | 与 Bash 版功能对等    |

**只学这些，不要碰**：类与继承、装饰器、多线程/asyncio、Django/Flask。第 6 月前都用不上。

### W12：自动化实战 + 月度考核

| 日   | 类型  | 内容                               | 当天达成结果            |
| --- | --- | -------------------------------- | ----------------- |
| D1  | A   | `subprocess` 调系统命令、捕获输出          | 能拿到命令的返回码和 stdout |
| D2  | A   | `requests` 调 REST API、处理 JSON 响应 | 能调一个公开 API 并解析    |
| D3  | B   | `paramiko` SSH 执行远程命令            | 能连一台机器执行命令拿结果     |
| D4  | B   | 并发执行：`concurrent.futures` 线程池    | 5 台机器并行执行不串行      |
| D5  | A   | 日志模块 `logging`、配置化脚本             | 脚本输出到日志文件，分级别     |
| D6  | 周末  | **月度考核：批量 SSH 巡检脚本**             | 见下方验收标准           |
| D7  | 周末  | 月复盘 + 把脚本传 GitHub + 写 README     | 仓库有 README 和使用示例  |

**W12 月度交付验收标准（必须全部满足）**

1. 从配置文件读取主机列表（不硬编码 IP），**主机直接填上面那 5 个容器的 `127.0.0.1:2221~2225`**
2. 并发连接，能拿到每台的 CPU/内存/磁盘/负载
3. 阈值可配置，超阈值打告警
4. 单台失败不影响其他机器，失败有记录
5. 有 logging，有 `-h` 帮助
6. `pylint` 无严重问题
7. README 写清用法和输出示例

> **两条安全与选型提醒**
> 
> **① `paramiko` 里这三行是生产禁忌，别形成习惯：**
> 
> ```python
> client.set_missing_host_key_policy(paramiko.AutoAddPolicy())   # ← 生产环境禁止
> ```
> 
> `AutoAddPolicy` 会**无条件接受任何主机密钥**，等于放弃中间人攻击防护。学习阶段图省事可以，但要在 README 里写明"此处为演示"，面试被问到"你怎么处理 host key"时你要能答：使用 `load_system_host_keys()` + `RejectPolicy`，配合 `ssh-keyscan` 预置或在首次部署时人工核对指纹。
> 另外 `paramiko` 是纯 Python 实现的 SSH，**并发数一高（几十台以上）性能和稳定性都会掉**，而且不支持 `~/.ssh/config` 的高级特性。生产里这个角色通常是 Ansible 的。
> 
> **② 建议调整顺序（可选，但对简历更好看）：**
> W12 用 paramiko 手搓并发 SSH，到 W24 才学 Ansible —— 相当于**先手工重造一遍轮子，再学成品**。两种做法都成立：
> 
> - 想**理解原理**：保持现有顺序，但一定在 README 里写清"手写版本是为了理解 SSH 批量执行机制，工程上我会用 Ansible，见 xxx 仓库"；
> - 想**简历更实**：W12 D3/D4 换成"先用 Ansible ad-hoc 跑通 `ansible all -m setup`，再用 paramiko 实现一遍等价逻辑"，两边对照写进 README。
> 
> **这个脚本是你简历上的第一个作品，认真做。** 后面第 6 月还会用到它。

---

## 第 4 月：Docker + Kubernetes

> **贯穿整月的建议**：从 W13 开始，**固定用一个你自己的应用**贯穿到 W26——比如一个 50 行的 FastAPI/Flask 服务，带 `/healthz` 接口，并连一个数据库。
> 理由：如果 W14 D6 用"W12 的巡检脚本"做镜像、W15 D6 又换成"W13 的 compose 应用"搬到 K8s、W22 D5 的部署对象再换一个、W24-W25 毕业项目又是新的一套，**对象一直在换，最后毕业项目讲不清楚，GitHub 也串不成一条线。**
> 统一成一个应用后，你的故事就是完整的一条：容器化 → 上 K8s → 配探针/资源限制 → CI/CD → 监控 → 毕业项目，**面试官能听懂**。

### W13：Docker 基础

| 日   | 类型  | 内容                                       | 当天达成结果                   |
| --- | --- | ---------------------------------------- | ------------------------ |
| D1  | A   | 容器 vs 虚拟机、镜像分层、`run/ps/images`           | 能解释为什么容器比 VM 轻           |
| D2  | B   | 容器生命周期 `start/stop/rm/exec/logs/inspect` | 熟练进入容器排查                 |
| D3  | A   | 数据卷 `-v`、bind mount、volume               | 能说出数据为什么容器删了还在           |
| D4  | B   | 网络 `bridge/host/none`、端口映射、容器互访          | 两个容器能通过自定义网络互相访问         |
| D5  | A   | `docker compose` 编排多容器                   | 写出一个 web+db 的 compose 文件 |
| D6  | 周末  | 实战：用 compose 起一套 WordPress/个人博客          | 浏览器能访问，数据持久化             |
| D7  | 周末  | 周复盘 + 交付：**compose 项目传 GitHub**          | 含 README 和启动命令           |

### W14：镜像构建

| 日   | 类型  | 内容                                                   | 当天达成结果                  |
| --- | --- | ---------------------------------------------------- | ----------------------- |
| D1  | A   | Dockerfile 指令 `FROM/RUN/COPY/WORKDIR/CMD/ENTRYPOINT` | 能说出 CMD 和 ENTRYPOINT 区别 |
| D2  | B   | 构建自己的 Python 应用镜像                                    | 镜像能跑起来                  |
| D3  | A   | 分层缓存、`.dockerignore`、构建优化                            | 改一行代码不用重装依赖             |
| D4  | B   | 多阶段构建、镜像瘦身                                           | 镜像从 1G 降到 200M 以内       |
| D5  | A   | 私有仓库：阿里云 ACR 推送拉取                                    | 镜像能推到 ACR 并拉下来          |
| D6  | 周末  | 实战：把 W12 的巡检脚本做成镜像                                   | 容器化后可一键运行               |
| D7  | 周末  | 周复盘 + 交付：**三个不同应用的 Dockerfile**                      | 三个都能跑，含优化说明             |

### W15：K8s 入门

| 日   | 类型  | 内容                                                | 当天达成结果                              |
| --- | --- | ------------------------------------------------- | ----------------------------------- |
| D1  | A   | K8s 架构：Master/Node、etcd、API Server、调度             | 能画出组件图和调用关系                         |
| D2  | B   | `k3d` 起集群、`kubectl` 基本操作                          | 集群 Ready，会 `get/describe/logs/exec` |
| D3  | A   | Pod：最小调度单元、生命周期、多容器                               | 能手写 Pod YAML 并创建                    |
| D4  | B   | Deployment：副本、滚动更新、回滚                             | 能演示 scale 和 rollout undo            |
| D5  | A   | Service：ClusterIP/NodePort/LoadBalancer、Endpoints | 能解释为什么 Service 选不中 Pod              |
| D6  | 周末  | 实战：把你的应用搬到 K8s                                    | 应用在 K8s 里能访问                        |
| D7  | 周末  | 周复盘 + 交付：**手写一套完整 YAML（不用 helm）**                 | 不复制粘贴写出 Deployment+Service          |

> **k3d 拉不到镜像时，别去改 daemon.json**
> **k3d/k3s 的节点用的是 containerd，不是 Docker**，它读的是自己的配置：
> 
> ```bash
> # 写一个 registries.yaml 给 k3d 用（注意格式和 daemon.json 完全不同）
> cat > /tmp/registries.yaml <<'EOF'
> mirrors:
>   "docker.io":
>     endpoint:
>       - "https://<你的专属ID>.mirror.aliyuncs.com"
> EOF
> k3d cluster create demo --registry-config /tmp/registries.yaml
> ```
> 
> 同理，k3s 集群落在 `/etc/rancher/k3s/registries.yaml`。**"我给 Docker 配了加速器，为什么 K8s 还是拉不动镜像"**是超高频卡点，也是面试可以讲的细节。

### W16：K8s 配置与存储

| 日   | 类型  | 内容                                                     | 当天达成结果             |
| --- | --- | ------------------------------------------------------ | ------------------ |
| D1  | A   | ConfigMap / Secret 三种用法                                | 能把配置从镜像里剥离出来       |
| D2  | B   | 探针：liveness/readiness/startup                          | 能解释就绪探针没过的现象       |
| D3  | A   | 存储：Volume、PV、PVC、StorageClass                          | 能说出 PV 和 PVC 的绑定关系 |
| D4  | B   | **流量入口：Ingress 概念 + Gateway API（k3d 自带 Traefik 可直接用）** | 能用域名（hosts）访问集群内服务 |
| D5  | A   | 资源限制 requests/limits、QoS、HPA 概念                        | 能解释 OOMKilled 的原因  |
| D6  | 周末  | 实战：给应用配齐探针/资源限制/入口                                     | 应用具备基本生产形态         |
| D7  | 周末  | 周复盘 + 交付：**一份生产级 YAML 模板**                             | 含注释，能复用            |

> **🚨 这里有个坑，而且是很多人正在踩的坑：Ingress NGINX 已经在 2026 年 3 月停止维护并归档了。**
> 
> 官方时间线：Kubernetes SIG Network + 安全响应委员会于 2025 年 11 月宣布 Ingress NGINX 退休，**2026 年 3 月起不再有任何发布、bugfix 和安全漏洞修复，项目进入归档状态**。到你现在这个时间点（2026 年 9 月），它已经是"半年没有任何安全补丁"的状态。
> 
> **注意别搞混**：**退役的是社区维护的 ingress-nginx 这个控制器，不是 Ingress API 本身。** Ingress 资源依然被支持、依然广泛使用（只是功能冻结了）。面试时把这句说清楚，是很加分的一句话。
> 
> **学习路径改成这样（由浅入深，成本都不高）：**
> 
> 1. **先懂 Ingress 概念**（它解决什么问题、host/path 怎么分流）——半小时看官方文档就够；
> 2. **实践用 k3d 自带的 Traefik**：k3s 默认就装了 Traefik，`k3d cluster create` 之后它就在跑，直接写 `Ingress` 资源就能用，**零额外安装**，而且 Traefik 是被维护的；
> 3. **再向前一步学 Gateway API**（推荐的长期方向）：它是 Ingress 的官方继任者，用 `GatewayClass` + `Gateway` + `HTTPRoute` 三层资源，把"基础设施团队管入口"和"业务团队管路由"分开了。本地 k3d 上装一个 conformant 实现（Envoy Gateway / Cilium / Istio 任选）跑通一个 `HTTPRoute` 即可。
> 4. **如果面试官问 Ingress Nginx**（很多公司还在用旧的），你要能答：我知道它 2026 年 3 月已归档，短期可以横向换到 Contour 等仍在维护的 Ingress 控制器（Ingress 资源不用改，但 `nginx.ingress.kubernetes.io/*` 这类私有 annotation 要重写），长期方向是 Gateway API。
> 
> **这句话就是你和其他自学者的差别**——别人还在照着 2024 年的教程敲 `ingress-nginx`，你已经在讲"为什么不能用它了"。

### W17：K8s 排障 + 月度考核

| 日   | 类型  | 内容                                    | 当天达成结果                                            |
| --- | --- | ------------------------------------- | ------------------------------------------------- |
| D1  | A   | 排障四件套方法论 + 常见状态含义                     | 能说出 Pending/ImagePullBackOff/CrashLoopBackOff 的原因 |
| D2  | B   | 实战：故意造 ImagePullBackOff 并修好           | 记录排查过程                                            |
| D3  | B   | 实战：故意造 CrashLoopBackOff 并修好           | 记录排查过程                                            |
| D4  | B   | 实战：故意造 Service 不通并修好                  | 记录排查过程                                            |
| D5  | A   | 日志与事件：`events`、`kubectl logs -p`、进入节点 | 能区分是应用问题还是集群问题                                    |
| D6  | 周末  | **月度考核**：给 3 个坏集群场景限时排障               | 3 题做出 3 题                                         |
| D7  | 周末  | 月复盘 + 交付：**K8s 排障手册（10 个案例）**         | 手册可直接用于面试复习                                       |

**W17 生死线**：能独立部署一个应用 + 排掉上述 3 类故障。过不了就延长 1 周，不要进第 5 月。

---

## 第 5 月：阿里云吃透

> 选阿里云。如果你所在城市是政企/信创为主（如部分二三线城市的政务云项目），可换华为云，**但只学一家**。
> 阿里云 → 华为云对照：ECS→ECS、VPC→VPC、SLB→ELB、RDS→RDS、OSS→OBS、RAM→IAM、云监控→CES、安全组→安全组。
> 
> **全程用标准 ECS 按量付费，不要用轻量应用服务器。** 原因见 1.6 的对照表——它不能加入自定义 VPC、不能挂标准 SLB、不支持 NAT/EIP，W18/W19/W21/W23 全部做不了。

### W18：账号与计算网络基础

| 日   | 类型  | 内容                                        | 当天达成结果          |
| --- | --- | ----------------------------------------- | --------------- |
| D1  | A   | 计费模式（包年包月/按量/抢占式）、地域可用区                   | 能算出三种模式的价格差异    |
| D2  | A   | **RAM 权限体系 + 费用预警设置**                     | 子账号创建成功，预警阈值已设  |
| D3  | B   | 购买**标准 ECS（按量付费，选最便宜的突发性能规格即可）** + SSH 登录 | 能登录自己买的服务器      |
| D4  | A   | VPC、交换机、路由表、网段规划                          | 能规划一个 3 子网的 VPC |
| D5  | B   | 安全组规则、入方向出方向、优先级                          | 能只放行必要端口        |
| D6  | 周末  | 实战：搭一个 VPC + 2 台不同子网的 ECS                 | 两台内网互通、一台有公网    |
| D7  | 周末  | 周复盘 + 交付：**手画 VPC 网络拓扑图**                 | 图含网段、路由、安全组     |

> **D3 买机器时，顺手把"释放"也规划好。** 阿里云没有"到期自动释放"给按量实例，但你可以：
> 
> - 用**节省停机模式**停掉（注意：节省停机只释放计算资源，**云盘继续计费**）；
> - 用完直接**释放实例**并勾选"释放云盘"；
> - 当天就在控制台「费用中心 → 资源实例」里核对一遍，确认没有"僵尸资源"。
>   **每周日复盘时加一条：检查有没有忘记释放的资源。** 这一步比省几块钱更重要的是——它是真实运维的日常（成本意识）。

### W19：负载均衡与对外服务

| 日   | 类型  | 内容                                        | 当天达成结果                  |
| --- | --- | ----------------------------------------- | ----------------------- |
| D1  | A   | SLB/CLB 原理、四层 vs 七层、健康检查                  | 能说出什么时候用四层什么时候七层        |
| D2  | B   | 配置 SLB 挂两台 ECS，轮询验证                       | 停掉一台，服务仍可用              |
| D3  | A   | NAT 网关、EIP、弹性公网 IP                        | 能解释为什么内网机器需要 NAT 出网     |
| D4  | B   | DNS 解析：域名购买、解析记录、TTL                      | 域名能解析到自己的 SLB           |
| D5  | A   | HTTPS：证书申请、SLB 上配证书 + **用 acme.sh 做自动续期** | 浏览器能通过 https 访问，且证书能自动续 |
| D6  | 周末  | 实战：域名 → SLB(HTTPS) → 2 台 ECS 全链路          | 全链路打通并有证书               |
| D7  | 周末  | 周复盘 + 交付：**全链路访问截图 + 架构图**                | 文档可入简历                  |

> **关于 D3 和 D5：这两处都跟钱和时间有关**
> 
> **D3 NAT 网关：这是整份计划里最容易产生"意外账单"的一步。**
> NAT 网关是**独立计费**资源，按量付费按规格和时长收费，**它不像 ECS 那样"停机就不怎么花钱"**。学习场景下它的价值只是"让内网机器出网"，而你完全可以用**一台小 ECS 开源地址转换**替代（成本低一个量级，而且是更硬核的练手）。
> **如果一定要开 NAT 网关：建完立刻做完实验，当天就释放**，别留过夜。同时记住三个"不做事也收钱"的资源特征：
> 
> - **按量付费 EIP**：绑定实例也收、**不绑定也收**（配置费）；
> - **SLB**：没流量也按规格计费；
> - **云盘/快照**：实例释放后云盘若保留、快照存在 OSS 里，都在计费。
>   这三条记牢，是面试也能讲的成本常识。
> 
> **D5 免费证书：周期比你想象的短得多。** 阿里云"个人测试证书（免费版）"的实际规则是：
> 
> - **有效期只有 3 个月，且不支持续费**，到期必须重新申请；
> - 同一实名主体**每自然年最多 20 张**（按实名主体共享，年底清零不累积）；
> - **只支持单域名，不支持通配符/多域名**。
> 
> 所以：**别把它当"申请一次管到毕业"的方案**。真正的运维做法是**ACME 自动续期**，用 `acme.sh` 或 `certbot` 走 DNS 验证自动签发、自动部署、自动 reload Nginx：
> 
> ```bash
> curl https://get.acme.sh | sh
> ~/.acme.sh/acme.sh --issue --dns dns_ali -d example.com
> ~/.acme.sh/acme.sh --install-cert -d example.com \
>   --key-file /etc/nginx/ssl/example.com.key \
>   --fullchain-file /etc/nginx/ssl/example.com.crt \
>   --reloadcmd "systemctl reload nginx"
> ```
> 
> **"我做过证书自动续期"比"我在控制台点过申请证书"在面试里值钱得多**，而且顺带练了 Nginx（W6.5 学过）。
> 
> **备案的时间账**（D4 顺带）：大陆节点 + 域名对外访问必须备案，7~20 个工作日，**且域名实名认证通过后通常还要等 3 天才能提交**。如果你想在 W19 就用上域名，**现在（第 4 月）就该去买域名做实名**。懒得等就用**中国香港节点**（免备案）。

### W20：数据库、存储、监控

| 日   | 类型  | 内容                                                | 当天达成结果                |
| --- | --- | ------------------------------------------------- | --------------------- |
| D1  | A   | RDS：购买、白名单、账号、连接                                  | 本地能连上 RDS             |
| D2  | B   | 把应用数据迁到 RDS，配置只允许内网访问                             | 应用从 RDS 读写正常          |
| D3  | A   | OSS：Bucket、权限、生命周期、CDN 概念                         | 能上传文件并生成可访问链接         |
| D4  | B   | 快照与备份策略、镜像制作、数据恢复演练                               | **做一次真实的回滚演练**        |
| D5  | A   | 云监控 + 告警规则（CPU/磁盘/连接数）+ **自建 Prometheus/Grafana** | 配一条告警并触发验证；本地监控栈能看到指标 |
| D6  | 周末  | 实战：给整套架构配齐监控和告警                                   | 有告警能收到通知              |
| D7  | 周末  | 周复盘 + 交付：**运维值班手册（怎么查怎么处理）**                      | 手册含 5 类告警处置流程         |

> D5 加了自建 Prometheus/Grafana 的入口，完整做法见第九部分。**云监控是"用别人的成品"，Prometheus/Grafana/node_exporter 是"自己搭一套"，后者才是面试考点。**

### W21：月度大项目 + 成本核算

| 日   | 类型  | 内容                                                    | 当天达成结果                        |
| --- | --- | ----------------------------------------------------- | ----------------------------- |
| D1  | A   | 高可用架构设计原则（无单点、分层、可扩展）                                 | 能指出自己架构里的单点                   |
| D2  | B   | 架构加固：多可用区、备机、健康检查                                     | 架构无单点                         |
| D3  | B   | 压测与验证：用 `ab`/`wrk` 压一下                                | 有压测数据和瓶颈判断                    |
| D4  | A   | 成本核算：账单分析、省钱手段                                        | 能算出月成本并说出优化点                  |
| D5  | A   | 整理架构文档（架构图、选型理由、成本）                                   | 文档 3~5 页                      |
| D6  | 周末  | **月度考核：不看教程复现整套架构**（用 W23 要学的"脚本化"思路，**不要求 3 小时内完成**） | 限时 **6 小时或分两天**，中间允许等待 DNS/证书 |
| D7  | 周末  | **释放所有付费资源** + 月复盘                                    | 账单归零，复盘完成                     |

> **关于 D6 的时限**：**不要按 3 小时算**。光"买 ECS → 装环境 → 配 SLB → 域名解析 → 证书签发"这条链路里，**DNS TTL 传播和证书签发审核都是不可压缩的外部等待时间**，加上首次操作必然要翻文档，3 小时会变成一场焦虑的自责。
> **改成**：限时 **6 小时，或拆成两天**，并明确"DNS 传播和证书签发的等待时间不计入"。考核要考的是**你的操作熟练度和排障能力**，不是"和公网打交道的运气"。

> ⚠️ **W21 D7 必须释放资源（SLB、ECS、NAT、RDS 别只停机，要释放）。** 停机照样收部分费用（尤其是云盘和按量 EIP）。

---

## 第 6 月：CI/CD + IaC + 毕业项目 + 求职

### W22：CI/CD

| 日   | 类型  | 内容                               | 当天达成结果        |
| --- | --- | -------------------------------- | ------------- |
| D1  | A   | CI/CD 概念、流水线阶段划分                 | 能画出提交到上线的流水线  |
| D2  | B   | **部署 Runner + 接入代码托管**（见下方方案对比）  | Runner 在线，能接单 |
| D3  | A   | 流水线语法：stage/job/script/artifacts | 能跑一个最简流水线     |
| D4  | B   | 流水线里构建镜像并推 ACR                   | 提交代码自动出镜像     |
| D5  | A   | 流水线里部署到 K8s（`kubectl apply`）     | 提交代码自动部署      |
| D6  | 周末  | 实战：完整流水线（构建→推镜像→部署→验证）           | 一次提交走完全流程     |
| D7  | 周末  | 周复盘 + 交付：**流水线配置 + 截图**          | 可入简历          |

> **⚠️ 不要在本地 Docker 里自建 GitLab（CE）常驻。在这个 100G + 24G 的机器上，这是一个会拖死整个计划的决定。**
> 
> 现实情况：GitLab 单机 Omnibus（含容器版）官方建议 **4 核 8G 起步**，实际跑起来镜像本身几个 G，**数据目录会持续膨胀**；而你这台机器同时还要跑 k3d 集群（W15-W17 的产物）。**100G 盘会在 W22 前后彻底见底，而且内存会被两家抢。**
> 
> **三个更省资源、且更贴近真实生产的替代方案（按推荐度）：**
> 
> | 方案                                             | 本地开销                   | 优点                                                                       | 缺点                |
> | ---------------------------------------------- | ---------------------- | ------------------------------------------------------------------------ | ----------------- |
> | **A. GitHub Actions + self-hosted runner（推荐）** | **极小**（只跑一个 runner 容器） | 免费额度大，云端跑流水线，**"混合 runner"是企业常见形态**；简历上写"GitHub Actions + 自托管 Runner"很标准 | 需要 GitHub 账号/网络可达 |
> | **B. Gitea + Gitea Actions（备选）**               | 小（Gitea 本体几十 MB）       | 完全本地、轻量、语法兼容 GitHub Actions                                              | 生态比 GitLab 小      |
> | **C. 自建 GitLab CE（原方案）**                       | **大（≥8G 内存、几十 G 磁盘）**  | 国内企业用得多，能写进简历                                                            | 会挤爆本地资源，和学习主线抢资源  |
> 
> **如果你仍然想学 GitLab（国内确实用得多）**：不要在本地常驻。改成**在第 5 月买的云 ECS 上起一个**，或者干脆只在 W22 期间临时起、做完就删，并**用 `--memory=6g` 限制**：
> 
> ```bash
> docker run -d --name gitlab --hostname gitlab.local \
>   --memory=6g --shm-size=512m \
>   -p 8929:8929 -p 2224:22 \
>   -v /srv/gitlab/config:/etc/gitlab \
>   -v /srv/gitlab/logs:/var/log/gitlab \
>   -v /srv/gitlab/data:/var/opt/gitlab \
>   gitlab/gitlab-ce:latest
> ```
> 
> 然后**在 W22 D7 立刻 `docker rm -f` 掉**，别留着。
> 
> **无论选哪个方案，D4 有个必须搞懂的技术选择**：Runner 在容器里执行 `docker build` 时，只有两条路——
> 
> - **挂载宿主 `docker.sock`**：`-v /var/run/docker.sock:/var/run/docker.sock`。简单，但**等于把宿主 root 权限交给了 runner**，任何能推代码的人都能接管你的机器。生产环境慎用，用了要说明原因。
> - **DinD（Docker-in-Docker）**：需要 `--privileged`，隔离更好但要求特权容器，且缓存管理更麻烦。
> 
> 这两句能讲清楚，面试官就知道你是真跑过流水线，不是只看过教程。

### W23：Terraform（IaC）

| 日   | 类型  | 内容                                        | 当天达成结果                      |
| --- | --- | ----------------------------------------- | --------------------------- |
| D1  | A   | IaC 思想、声明式 vs 命令式、Terraform 工作流           | 能说出 `plan/apply/destroy` 作用 |
| D2  | B   | 安装 + `aliyun/alicloud` provider + 第一台 ECS | 用代码创建出 ECS                  |
| D3  | A   | 变量、输出、`terraform.tfvars`                  | 参数化配置                       |
| D4  | B   | State 管理、remote backend（`backend "oss"`）  | 能说出 state 为什么不能进 Git        |
| D5  | A   | 用 Terraform 建 VPC + 安全组 + SLB             | 一条命令拉起网络层                   |
| D6  | 周末  | 实战：把 W18-W19 的架构用代码重写                     | 代码可一键创建和销毁                  |
| D7  | 周末  | 周复盘 + **务必 `terraform destroy`**          | 验证资源全部释放                    |

> ⚠️ **AccessKey 绝不写进 `.tf` 文件，也绝不提交 Git。** 用环境变量 `ALICLOUD_ACCESS_KEY` / `ALICLOUD_SECRET_KEY`；`*.tfvars` 和 `*.tfstate` 必须进 `.gitignore`。
> 
> **三个细节**
> 
> 1. **provider 准确名称是 `aliyun/alicloud`**（不是 `aliyun`），在 `required_providers` 里写 `source = "aliyun/alicloud"`。
> 2. **`terraform destroy` 对"包年包月"资源只能销毁、不退费。** 只有按量付费（pay-as-you-go）的资源才是真正"一键创建、一键清零账单"。这也是为什么 W18 D3 要用按量付费——**IaC 的整个心智模型建立在"资源可随时销毁"上**，预付资源会破坏这个前提。
> 3. **Terraform 管不了轻量应用服务器的那套网络**（VPC/安全组/SLB），这也再次说明 W18 必须用标准 ECS。

### W24：Ansible（轻量）+ 毕业项目启动

| 日   | 类型  | 内容                               | 当天达成结果                     |
| --- | --- | -------------------------------- | -------------------------- |
| D1  | A   | Ansible 原理、inventory、ad-hoc 命令   | 能对多台机器批量执行命令               |
| D2  | B   | Playbook、模块（copy/service/apt）、变量 | 写出一个安装 nginx 的 playbook    |
| D3  | B   | Role 概念 + `--check` 演练模式         | 能说出 Ansible 和 Terraform 分工 |
| D4  | A   | 毕业项目设计：画架构图、写选型理由                | 架构图定稿                      |
| D5  | B   | 毕业项目：用 Terraform 拉起基础设施          | 基础设施一键创建成功                 |
| D6  | 周末  | 毕业项目：Ansible 初始化服务器配置            | 机器自动装好环境                   |
| D7  | 周末  | 周复盘 + 项目进度复盘                     | 进度符合计划                     |

> Ansible 到此为止。**不要深入 role 生态、不要学 AWX。** 它在你的求职里只是加分项。
> 
> D5 用 Terraform 拉基础设施，D6 用 Ansible 装环境 —— **这个分工要能一句话讲清**：Terraform 管"资源是否存在"（声明式、有 state），Ansible 管"机器里长什么样"（过程式、无 state）。面试常问"这两个是不是重复了"，答不上来会显得只是跟着教程走。

### W25：毕业项目收尾

| 日   | 类型  | 内容                            | 当天达成结果    |
| --- | --- | ----------------------------- | --------- |
| D1  | B   | 毕业项目：CI/CD 流水线接入              | 提交代码自动部署  |
| D2  | B   | 毕业项目：监控告警 + 备份策略              | 告警能触发     |
| D3  | B   | 毕业项目：把 W12 巡检脚本接进定时任务         | 巡检结果有记录   |
| D4  | B   | 故障演练：故意宕一台机器，验证可用性            | 服务不中断     |
| D5  | A   | 写项目文档：架构图、部署步骤、踩坑记录、成本        | 文档 5~10 页 |
| D6  | 周末  | GitHub 整理：3 个仓库 + README + 截图 | 仓库对面试官友好  |
| D7  | 周末  | 周复盘 + **释放云资源**               | 账单受控      |

> **D3 的补充**：接进定时任务时，**用 `systemd timer` 而不是 cron**（呼应 W4 D3）。理由很实际：你演示时机器可能不在开机状态，`OnCalendar=` + `Persistent=true` 会补跑，面试演示不会翻车。

### W26：求职冲刺

| 日   | 类型  | 内容                      | 当天达成结果       |
| --- | --- | ----------------------- | ------------ |
| D1  | A   | 简历改写：突出实施经验 + 6 个月项目    | 简历完成一版       |
| D2  | A   | 项目讲解稿：3 分钟讲清架构和难点       | 能脱稿讲完        |
| D3  | A   | 高频面试题梳理（Linux/网络/K8s/云） | 整理 50 题并自答一遍 |
| D4  | B   | 模拟面试：自问自答并录音回听          | 找出 3 个表达漏洞   |
| D5  | B   | 开始投递（目标岗位见第十部分）         | 至少投 20 家     |
| D6  | 周末  | 面试复盘机制建立（记录被问倒的问题）      | 复盘表模板        |
| D7  | 周末  | 总结：6 个月收获 + 下一阶段计划      | 明确的下一步       |

> **不要等"全部学完"再投简历。** W26 就开始投，面试反馈是最好的学习指南。
> 
> **"W26 再开始投简历"太晚了。** 见第十部分：**从 W1 起每周花 30 分钟看招聘 JD，把词频统计出来**。你花 6 个月自学，最后发现自己没学 JD 上写着的东西，是最贵的失误。

---

## 第五部分：检查点体系

| 层级  | 频率             | 达标定义               | 不达标怎么办        |
| --- | -------------- | ------------------ | ------------- |
| 日   | 每天             | 当天产出落到笔记/代码里，报错有记录 | 当天补完，不拖到次日    |
| 周   | 每周五            | **不看任何资料**能完成周检查点  | 周末只做补救，不进新内容  |
| 月   | 每月最后周末         | 限时完成月度考核 + 交付物入库   | 延长 3~7 天补，不硬冲 |
| 阶段  | W4 / W12 / W17 | 生死线通过              | 停下来补，宁可延期 2 周 |

**周复盘三问（每周日写）**

1. 本周哪件事真的动手做了？（没动手 = 没学）
2. 哪个知识点我还讲不出来？（讲不出 = 没懂）
3. 下周要砍掉什么？（防止摊大饼）

**第四问（每周日，30 分钟）**

**4. 本周招聘网站上，目标岗位反复出现的关键词有哪些？我学了几个？**

做法：打开拉勾/BOSS/猎聘，搜"云运维""平台运维""SRE"，**只读 JD 不投递**，把出现频率最高的技术词记下来。跑 4 周你就会发现哪些是"必备"，哪些是"加分"——**用它来校准第 6 月的优先级表，比自己猜准得多**。

---

## 第六部分：时间不够时的取舍规则

真实生活一定会有加班和意外。按这个顺序砍，**从下往上砍**：

| 优先级 | 内容                                                 | 可否砍                      |
| --- | -------------------------------------------------- | ------------------------ |
| P0  | Linux 排障、网络基础、Docker                               | **绝不能砍**                 |
| P0  | 阿里云实操与项目                                           | **绝不能砍**（求职主力卖点）         |
| P0  | **Nginx 反向代理 + 502/504 排查**                        | **绝不能砍**（面试第一高频）         |
| P1  | K8s 核心对象与排障                                        | 缩减深度，不能删                 |
| P1  | Shell/Python 自动化                                   | 缩减深度，保住 W12 脚本           |
| P1  | **Prometheus + Grafana 基础**                        | 缩减到"能搭起来 + 看懂一张图"，不能删    |
| P2  | Terraform                                          | 可只学基础，但别删                |
| P2  | CI/CD                                              | 可降到"能跑通一条流水线"            |
| P3  | **Ansible**                                        | **最先砍**                  |
| P3  | **证书考试**                                           | **砍掉，省钱省时间**（除非目标公司明确要求） |
| P3  | Jenkins（如果已选 GitHub Actions/GitLab CI）             | 砍                        |
| P4  | K8s Operator/CRD/CNI 原理、Service Mesh、Serverless、多云 | **完全不学**                 |

> **两处需要说明**
> 
> 1. **Nginx 定在 P0。** 它是日志分析和 K8s 入口的底座，而且运维岗问"502 和 504 的区别""反代怎么配 TLS"答不上来，前面 5 个月的说服力会掉一半。
> 2. **Terraform 不是"加分项"，是基本要求。** 2026 年的云运维岗，IaC 已经从"加分项"变成"基本要求"，**它是入场券，不是差异化**。真正的差异化是你的组合：**阿里云实操 + K8s 排障 + IaC + 实施经验带来的"能落地"**，而不是某一项技能本身。

**加班到只剩 1 小时的日子**：只做"复盘 10 分钟 + 敲 50 分钟"，不要开新知识点。

---

## 第七部分：必然踩到的坑与解法

| 坑                       | 症状                              | 解法                                                                   |
| ----------------------- | ------------------------------- | -------------------------------------------------------------------- |
| 项目路径混乱                  | Windows 和 Ubuntu 两边都放一份，改了哪份不知道 | **代码只放 Ubuntu 的 `~/projects`**，走 Git 同步                              |
| 双系统 IP 变了               | 重启后 SSH、脚本全部失效                  | 路由器里做 DHCP 保留或设静态 IP                                                 |
| Ubuntu 读 Windows 盘只读    | 快速启动/休眠没关                       | `powercfg /h off`，见 1.5 ①                                            |
| 双系统时间差 8 小时             | RTC 模式不一致                       | Windows 侧 `RealTimeIsUniversal=1`，Ubuntu 侧 `set-local-rtc 0`，见 1.5 ② |
| 磁盘满（100G）               | 第 4 月突然写不进文件                    | 每周 `docker builder prune -af` + `docker image prune` + 清理旧集群，见 1.3   |
| **挂载新盘后 Docker 数据"消失"** | 挂载盖住 `/var/lib/docker` 旧数据      | 先停 Docker → rsync 迁移 → 再挂载，见 1.5④                                    |
| **`/etc/fstab` 写错**     | 重启进 emergency mode              | 备份 fstab；用 UUID；`mount -a` 先验证；维护模式下注释掉                              |
| **ufw 拦不住 Docker 端口**   | 防火墙 deny 了，`-p` 发布后全世界可访问       | `-p 127.0.0.1:port:port`，或用 `DOCKER-USER` 链，见 W7                     |
| **k3d/K8s 拉不动镜像**       | 给 Docker 配了加速器，集群还是超时           | k3s 用 containerd，要配 `registries.yaml`，见 W15                          |
| **iptables 规则重启即失**     | 规则明明加了，重启就没了                    | `iptables-persistent` / `nftables.conf`，见 W7                         |
| 新内核网络诡异                 | iptables 规则看着对但不生效              | 搜索时带上 Ubuntu 26.04 和内核版本号                                            |
| 镜像拉不下来                  | `pull access denied` / timeout  | 换**阿里云专属**加速地址；别死磕超 30 分钟，见 1.2②                                     |
| **改坏 sudoers 被锁死**      | 提权通道全断，什么都干不了                   | `visudo` + 留 root shell + 备份 sudoers；救援见 1.8                         |
| **OOM 把系统压死**           | 做压测时桌面/sshd 被杀                  | `systemd-run --scope -p MemoryMax=` 限住压测进程，见 W3                      |
| 只看视频不动手                 | 感觉会了，一做全废                       | 输入时长 ≤ 输出 1/2，用计时器管                                                  |
| K8s 教程版本太老              | YAML 照着敲全报错                     | 以官方文档为准，别看 2023 前教程                                                  |
| **还在学 ingress-nginx**   | 跟的教程已过时，学的组件已归档                 | 2026-03 已停维护，改 Gateway API/Traefik，见 W16                             |
| **云资源忘记释放**             | 账单悄悄扣费                          | 建资源当天就设预算预警；**NAT/EIP/SLB 尤其要当天释放**；每月底清点                            |
| **以为停机就不花钱**            | 省了停机费，账单还是涨                     | 按量 EIP（未绑定也收）、SLB（闲置按规格）、云盘/快照都在计费                                   |
| AccessKey 泄露            | 账号被盗刷（真实高发）                     | 用环境变量；永不提交 Git；用子账号 + 最小权限                                           |
| 学了三周想放弃                 | 正常现象，第 3-5 周是放弃高峰               | 提前知道；那天只做 30 分钟也算没断                                                  |
| 摊大饼                     | 同时学 5 个方向，全都半途                  | 严格执行 P0-P4 优先级                                                       |
| 追求"学完再投简历"              | 永远投不出去                          | **W1 就每周看 JD，W26 开始投**                                               |

---

## 第八部分：投入预算

| 项目                  | 金额               | 备注                                              |
| ------------------- | ---------------- | ----------------------------------------------- |
| 内存升级                | 0 元              | 已有 24GB，无需升级                                    |
| 云服务器（**标准 ECS，按量**） | **400~800 元**    | 只在 W18-W21、W24-W25 开；**不要用轻量应用服务器**（见 1.6）      |
| NAT 网关              | **0~60 元，或直接不用** | 按量计费偏高，建议用一台小 ECS 自建替代                          |
| 域名                  | 30~60 元          | 首年，**建议 W16-W17 就买好并做实名**（备案要 3 天等待期）           |
| SSL 证书              | 0 元              | 但**免费证书只有 3 个月、不支持续费、每年 20 张**，务必配 acme.sh 自动续期 |
| 课程                  | 0~500 元          | B 站免费课足够                                        |
| 书籍                  | 0~200 元          | 2~3 本纸质书                                        |
| 证书考试                | 0 元              | **建议不考，省 1000+ 元**                              |
| **合计**              | **约 450~1600 元** | 含云资源、域名与书籍                                      |

**省钱铁律**：

1. 云上所有实验做完立刻释放。**停机不等于不收费。**
2. **按量 EIP 未绑定也收配置费、SLB 闲置也按规格计费、NAT 网关按量最贵**——这三样是账单刺客，当天用完当天释放。
3. 每周日在「费用中心 → 资源实例」里核对一次，**确认没有僵尸资源**。

---

## 第九部分：两个必补模块

> 这两个模块是 2026 年运维岗的标配，但很容易被自学者跳过。都用本地 Docker 跑，**零云成本**。

### 9.1 Nginx 实操（已并入 W6.5，此处仅说明定位）

见 W6 之后的 **W6.5 模块**。定位：**它不是"顺便学学"，是面试第一高频考点。** 学完你要能立刻答出：

- 反向代理和负载均衡的区别？
- 502 和 504 分别是什么原因，你怎么查？
- 怎么给 Nginx 配 HTTPS，证书怎么续？
- `proxy_pass` 后面带不带 `/` 有什么区别？（这个细节很多人答不上）

### 9.2 Prometheus + Grafana 本地监控栈（1~2 天，安排在 W20 期间）

**为什么必须有**：只用阿里云云监控的话，那是**别人做好的成品**，你学不到原理，面试也讲不出细节。而 Prometheus + Grafana 是当下运维/SRE/云原生岗**最常写在 JD 里的技术栈**，而且本地 docker compose 就能完整搭起来。

**动手清单（一个下午可完成）：**

```bash
mkdir -p ~/projects/monitoring && cd ~/projects/monitoring
# 用 compose 起 prometheus + grafana + node_exporter（采集宿主指标）
```

1. 起 **node_exporter** 采集本机指标（CPU/内存/磁盘/网络/文件系统）；
2. 起 **Prometheus**，写 `prometheus.yml` 抓取 node_exporter 和自身；
3. 在 Prometheus 里手写一次 PromQL，回答三个问题：
   - 本机当前 CPU 使用率是多少？（`rate(node_cpu_seconds_total{mode="idle"}[5m])`）
   - 哪个文件系统剩余空间小于 20%？
   - 过去 1 小时磁盘写入速率趋势？
4. 起 **Grafana**，接 Prometheus 数据源，**导入 Node Exporter Full 面板（ID 1860）**，看一遍所有指标并挑 3 个能讲清的；
5. 配一条 **Alertmanager 告警规则**（比如磁盘 > 80%），让它真的触发一次。

**交付**：一个 `docker-compose.yml` + `prometheus.yml` + `alert.rules.yml` 的仓库，README 里写清"我为什么选 pull 模型而不是 push"。

**面试收益**：能讲清 `exporter → Prometheus 拉取 → 告警规则 → Alertmanager 通知 → Grafana 展示` 这条链，以及"监控和告警不是一回事"。**这比"我在阿里云控制台配了个 CPU 告警"含金量高一个量级。**

### 9.3 顺手补齐：几个面试常问的小点

| 点                        | 你要能答的                                                                |
| ------------------------ | -------------------------------------------------------------------- |
| **防火墙规则持久化**             | iptables 规则重启就丢，怎么落地？（`iptables-persistent` / `nftables.conf`）       |
| **Docker 与防火墙的关系**       | Docker 为什么会绕过 ufw？三种解法是什么？                                           |
| **containerd vs Docker** | K8s 节点上用的是 containerd，镜像加速配置点在哪？（`registries.yaml`，不是 `daemon.json`） |
| **swap 与 kubelet**       | 为什么 K8s 要求关 swap？k3d 为什么不受影响？                                        |
| **备份的 3-2-1 原则**         | 什么是 3-2-1？你本地环境的备份符合吗？                                               |

---

## 第十部分：求职目标修正

很多人的默认想法是投"**云交付 / 云运维 / 云实施**"这三个方向。

**问题在于："云交付"和"云实施"恰恰是出差最多、最偏现场执行的岗位形态。** 而你的取向很明确：**不喜欢现场实施（出差多、琐碎、偏执行），希望尽量少出差、偏产品/平台侧。**

**学了 6 个月，最后投回一个和现在同一类型（甚至更累）的岗位，等于白折腾。** 所以目标要重新排：

| 优先级    | 岗位方向                  | 特征             | 说明                       |
| ------ | --------------------- | -------------- | ------------------------ |
| **首选** | **云平台运维 / 平台运维**      | 坐班为主，管内部平台和资源池 | 最匹配"少出差 + 技术纵深"          |
| **首选** | **云原生运维 / K8s 运维**    | 坐班、偏技术、排障为主    | 你的 K8s 排障手册 + 巡检脚本能直接用   |
| **首选** | **运维开发（初级）/ SRE（初级）** | 坐班、要写脚本和自动化    | 你的 Python 自动化 + IaC 是敲门砖 |
| **次选** | 云安全运维 / 云监控告警岗        | 坐班，偏值守与分析      | 门槛略低，可作保底                |
| **次选** | 云技术支持（HOT/内部支持）       | 部分需轮班，出差少      | 电话/工单支持，不用驻场             |
| **避开** | **云交付 / 云实施 / 驻场工程师** | **出差多、偏现场执行**  | 与你现在的痛点重合，**别往回投**       |

**配套动作（两条，现在就做）：**

1. **W1 起，每周日花 30 分钟看 JD**（见第五部分新增的第四问）。目标：**在 W12 之前攒够 50 个"首选岗位"的 JD 文本，统计高频关键词**，用它来确认你后面学的方向没跑偏。

2. **W26 之前把"面试官一定会追问的一件事"想好**：
   
   > "你原来做实施，为什么现在做运维？"
   
   这道题答不好，前面的技术都会打折。**建议的答法**（不是话术，是你真实的迁移逻辑）：
   
   > "我在现场做了 14 个月，最深的体会是现场问题有一半以上最后都落在'部署环境不一致'和'没有可观测性'上。我后来自己动手做了一套批量巡检脚本和一套监控，把原本靠人肉排查的活变成了自动告警，这让我确认自己真正想做的是把系统做得更稳，而不是一遍遍去现场救火。所以这 6 个月我系统补了 Linux、网络、容器、K8s 和云上实操，也自建了监控和 CI/CD。"
   
   注意最后那句的关键词：**自建**（不是"配置过"）、**系统补**（不是"看过教程"）。配合你 GitHub 上那 3 个仓库，这个故事就闭环了。

---

## 附录：学习资源清单

> 资源会过时，以下按*类型*选，不要贪多。**每个主题只看一套，看完就动手。**

| 主题        | 资源类型       | 建议                                                                              |
| --------- | ---------- | ------------------------------------------------------------------------------- |
| Linux 系统  | 视频课（B 站）   | 选播放量高、2023 年后更新的系统课，看完目录结构+权限+进程即可                                              |
| Linux 系统  | 书          | 《鸟哥的 Linux 私房菜·基础学习篇》当字典查，不要通读                                                  |
| Linux 命令  | 书          | 《Linux 命令行大全》选读                                                                 |
| 网络        | 书          | 《图解 TCP/IP》《网络是怎样连接的》——这两本最适合零基础                                                |
| 网络        | 工具         | Wireshark 官方示例抓包文件，跟着分析                                                         |
| **Nginx** | 官方文档       | nginx.org 的 Beginner's Guide + `proxy_pass` 那页（务必看清尾部 `/` 的行为）                  |
| Shell     | 教程         | 任选一套 Bash 入门，重点在动手不在看                                                           |
| Shell     | 工具         | ShellCheck（必用）、`explainshell.com`                                               |
| Python    | 教程         | 廖雪峰 Python 教程（只看基础篇，不看框架）                                                       |
| Docker    | 官方文档       | docs.docker.com 的 Get Started                                                   |
| K8s       | 官方文档       | kubernetes.io 的 Tutorials，**以官方为准**                                             |
| K8s       | 概念         | 官方博客《Ingress NGINX Retirement: What You Need to Know》**必读**（理解了它，你就领先一半年限比你高的人） |
| K8s       | 工具         | `killer.sh`（CKA 模拟环境，也可当练习场）                                                    |
| **监控**    | 官方文档       | Prometheus 的 Getting Started + Grafana 导入面板 1860                                |
| 阿里云       | 官方文档 + 控制台 | 边点边看文档，比看视频快                                                                    |
| 阿里云       | 产品对比表      | 搜"轻量应用服务器 vs ECS 对比"，**先看清限制再买**（这里最容易踩坑）                                       |
| Terraform | 官方文档       | registry.terraform.io 的 `aliyun/alicloud` provider 示例                           |
| 面试        | 自整理        | 自己整理的 50 题 + 排障手册，比买题库有用                                                        |

---

## 关于心态

计划本身不重要，**"每天那 2 小时有没有真的动手"**才重要。

26 周里你一定会有一周完全没学。没关系，**断一周不叫失败，断两周才叫失败**。第 3~5 周和第 14 周是放弃高峰，提前知道，那天哪怕只敲 30 分钟命令，也算没断。

**6 个月后的目标不是"成为云计算工程师"，是"拿到一张入场券"。** 进门之后，再花 1~2 年做深。

---

## 随身带着的三句话

- **`/var/lib/docker` 要挂新盘，先挂盘再装 Docker** —— 顺序错了数据就"消失"。
- **Docker 会绕过 ufw，`-p` 发布就是对全世界开放** —— 这是生产事故第一名。
- **ingress-nginx 已经退役了** —— 你按 2024 年教程学的东西，2026 年可能已经在教错。
