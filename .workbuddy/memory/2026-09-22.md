# 2026-09-22

## 任务一：为每节课配「面试题库」，并按真实依据标重点

新建 `面试题库/` 目录：第 1 课（24 题）、第 2 课（41 题）、`题库规范.md`（唯一标准）。
`学习日志.md` 新增「面试题库」索引表（含题量 / ★★★ / 🔧 / ☆ 计数）+ 防膨胀规则。

### 关键决策（后续每课一律沿用）

1. **评级四级**：`★★★ 必背` / `★★ 理解` / `🔧 生产必会` / `☆ 非面试题`。
   后两级是专门拆开"面试会问"与"企业真遇到"这两件事 —— 用户明确要求重点标记 = 面试高频 **且** 真实生产场景。
2. **★★★ 必须有外部依据**（公开面经/真题命中，或公开事故复盘里的直接根因）；查不到依据的一律降级。
   **我为了教学自造的题必须标 ☆ 并注明"非面试题"**，不许混进 ★★★。
3. **组内按星级从高到低排序**（用户 2026-09-22 明确要求）：
   `★★★ → ★★ → 🔧 → ☆`；同级按**题号升序**；**题号永不重排**（答案区按题号对应，排序只改显示顺序）。
4. **每课题目上限 30 题**；必须含「母题索引」（几十题压缩成几个原理）+「考前 48h 必背 TOP10」；
   复习用**三轮过滤法**（① 逐题口述标记 ② 只做不会的 ③ 考前只看 TOP10）。

### 已用外部依据（来源库，后续复用）

| 代号 | 来源 | 命中内容 |
| --- | --- | --- |
| 面经A | https://dbaplus.cn/news-134-6621-1.html | 内存精细化监控、buffer 与 cache 区别及调优、磁盘 IO 瓶颈定位 |
| 面经B | https://blog.csdn.net/weixin_38889300/article/details/155188036 | rm 删文件后 df 不释放、drop_caches 风险、inode/硬链接、Docker 与 VM 对比、UnionFS 分层 |
| 事故C | https://cloud.tencent.cn/developer/article/2536969 | K8s 磁盘故障：`/var/lib/docker/overlay2` 下某容器日志占 52GB 导致磁盘耗尽 |
| 官方D | https://wiki.debian.org/Docker | 原文：Docker group membership is more dangerous than sudo |

### 踩坑记录（重要，避免复发）

- **给题目加行首标记破坏了 Markdown 语法**：写成一行的 `【★★】1. 问题` → 行首不再是 `1.`，Markdown 不认列表项，**整组被合并成一个段落**；答案区的 `【★★★】### 14 ·` 同理让标题失效。用户反馈"四组题目糊成一团、没有格式"。
- **修正与规范**：问题行必须是 `- [ ] **N.** 【标记】文本`；答案标题必须是 `### 【标记】N · 标题`。
  **标记永远放在语法元素之后，绝不放在行首。**
- **自查不到位**：改完只看了源码没看渲染效果。规范里增加"交付前必须在渲染视图实际看过一眼"。
- **批量脚本要限定区域**：加标记的正则 `^\d+\.` 误伤了附录「三个连环问」的 1./2./3.，已回退。

以上两条已写进 `面试题库/题库规范.md`（第 3 节硬规则、第 8 节踩坑记录）。

---

## 任务二：澄清题量焦虑（用户问"两章就 60 多题，学完不得几千条？"）

已向用户说明并落进题库结构：原始累加约 1000~1200 题 → 去重后独立知识点约 300 个 → 母题约 70 个 →
必须背死的八股约 100 条 → 单场 45 分钟面试实际问到 10~20 题。
**题库是覆盖地图，不是背诵清单**；题量前重后轻，母题跨课复用（后续只引用、不重复造题）。

---

## 待办 / 进度

- 第 1 课（W1 D1 机器体检）与第 2 课（装工具 + Docker）：**用户尚未贴回实验输出**，讲义与题库已就绪。
- 第 3 课 · SSH + 固定 IP：讲义 + 面试题库同步产出（题库须遵守 `题库规范.md`）。
- 每课交付固定三件套：**讲义（学习日志.md）+ 面试题库 + 索引表同步**。
- 教学模式约定沿用 2026-09-20 记录（每课一小步、给命令+预期输出+分支处理、不追问开放问题）。

---

## 任务三：面试题库精简为四部分（用户要求）

**用户原话**：`这个面试题库文档删减不需要的描述不要加上，只保留课题，母题索引，问题清单和答案四个部分就可以`

- **题库文件最终只保留四部分**（顺序固定）：① 课题（H1 标题）② 母题索引（表）③ 问题清单（勾选列表）④ 参考答案。
- **删掉**：用法说明、评级口径、★★★ 依据表、诚实声明、统计小计、格式说明、自测记录表、附录连环问、已知缺口。
- **依据不丢**：四级标记的依据统一搬到 `面试题库/题库规范.md` 第 5 节「依据库」（面经A/B、事故C、官方D），题库里只留标记本身。
- 已更新 `题库规范.md`（第 2 节结构、第 8 节自检清单、第 9 节踩坑记录全部按"四部分"重写）。
- 本次踩的第二个坑：`## 母题索引` 标题带括号导致重建时匹配失败、母题表格一度丢失 → **重建脚本要按前缀匹配，不能按全等匹配**。

---

## 任务四：文档转 PDF（用户："字体缩小点，转 PDF 要 9 页太多了"）

- **关键认知**：字号不在 md 里，由导出工具决定 → 直接做导出管线，而不是改 md 内容。
- 管线：`pdf/md2html.mjs`（自写 md→HTML，零外部依赖）→ Chrome 无头 `--print-to-pdf`。
  样式：`pdf/print.css`（9pt 基线）/ `pdf/print-8pt.css`（8pt + 9mm 边距）；一键脚本 `pdf/export.ps1`。
- **页数**：第 2 课题库 9pt 单栏 7 页 → **8pt 双栏 4 页**；第 1 课 **3 页**；学习日志 **4 页**（单栏）。
- 环境事实：本机**无 pandoc / wkhtmltopdf**，有 Chrome + Edge（无头打印）与 Node。
- 两个操作坑：① 生成脚本里的正则含反引号，写进模板字符串会截断 → **生成脚本一律用 String.fromCharCode(96)**；
  ② 清理用的正则 `/-(双栏|8pt|8pt双栏)\.pdf$/` **把要保留的 8pt 成品一起删了**，且目标文件被占用时 unlink 报 EBUSY
  → **清理白名单要显式列举，不要用模糊后缀匹配**。

### 补充（用户偏好）：PDF 只用小字号，不要双栏

- 用户原话：**"只是所字体缩小不要搞两行排列，字体缩小就行，页数没有硬性要求"**。
- **PDF 一律单栏**，省页只靠字号（默认 8pt）与页边距，**禁止再用 column-count 双栏**。
- `pdf/export.ps1` 默认 `-Cols 1`；`pdf/print.css` 里的 `body.cols2` 规则保留但不启用。

### 补充（用户偏好）：PDF 字号定为 9pt

- 用户问"字体现在是多大，可以稍微大一点" → 原来 8pt，现改为 **9pt（小五号）**，仍单栏。
- 预设文件：`pdf/print-9pt.css`（默认）/ `pdf/print-8pt.css`（更小）/ `pdf/print.css`（9pt 宽松行距）。
- 字号对照：六号 7.5pt ＜ **8pt** ＜ 小五号 **9pt** ＜ 五号 10.5pt（Word 常規正文）。
- `pdf/export.ps1` 默认 `-Preset pdf\print-9pt.css`、`-Cols 1`。

### 补充：★ 标记加粗（字体回退修正）

- 现象：`★`(U+2605) 微软雅黑无此字形 → 浏览器回退到符号字体，**标记看起来比正文小**，而标记恰恰是最该被看见的东西。
- 修法：`md2html.mjs` 的 inline() 把 `【★★★|★★|🔧|☆】` 包成 `<span class="mk">`；`print.css` 加 `.mk{font-weight:700;font-family:"Segoe UI Symbol","Noto Sans Symbols 2",...}`。
- 代价：标记变宽 → 页数各 +1（学习日志 5 / 第 1 课 4 / 第 2 课 6）。用户已明确"页数没有硬性要求"，故接受。
