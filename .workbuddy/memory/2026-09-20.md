# 2026-09-20

## 任务：审阅并修订《云计算6个月转行计划》

以资深 Linux 运维视角审阅 `云计算6个月转行计划.md`，发现 **25 处缺陷**，已原地重写为 v2（同文件，1217→1554 行，含完整修订记录附录）。

### 缺陷分类与关键结论（重要，后续迭代沿用）

**A 类 事实错误 6 处**（都已联网核实）：
- A1 Docker 镜像加速：`dockerproxy.com` 已失效/转只读；`mirror.ccs.tencentyun.com` **仅腾讯云内网稳定**，家用宽带上没用 → 改用阿里云 ACR 个人实例**专属**地址。
- A2 云服务器无法访问本地 `192.168.x.x`（NAT 后），v1 此条逻辑不成立 → 用 `ssh -R` 反向隧道（+`GatewayPorts yes`）。
- A3 **轻量应用服务器不能加入自定义 VPC、不能挂标准 SLB（只有独立"轻量负载均衡"，60元/月、仅 L7、限北京/杭州/深圳）、只能包年包月、不支持 NAT/EIP** → 学习期必须用标准 ECS 按量付费。
- A4 阿里云免费 SSL 证书：**仅 3 个月、不支持续费、每实名主体每年 20 张、仅单域名不支持通配符** → 改用 acme.sh 自动续期。
- A5 **Ingress NGINX 已于 2026 年 3 月停止维护并归档**（2025-11 官宣）→ 改 Gateway API / k3d 自带 Traefik。注意：退役的是 ingress-nginx 控制器，**不是 Ingress API**。
- A6 `timedatectl set-local-rtc 1` 是 systemd 标注"不完全支持"的写法 → 改 Windows 侧 `RealTimeIsUniversal=1` + `set-local-rtc 0`。

**B 类 自相矛盾 6 处**：安装形态摇摆（补"第 0 步"+1.5-A 虚拟机分支）；WSL 残留 2 处；W12 多台靶机来源未写（补 Docker 起 5 个 sshd 容器，2221~2225）；**`/var/lib/docker` 挂新盘的顺序错误（先装 Docker 再挂盘会盖住旧数据）**；第 5 月负载超标。

**C 类 关键缺失 7 处**：无备份/回滚（补 1.8 + GRUB `init=/bin/bash` 救援）；改 sudoers 无安全网；**全程没教 Nginx**（补 W6.5，含亲手造 502/504）；**无 Prometheus+Grafana**（补 9.2）；缺防火墙持久化 + **Docker 绕过 ufw**；W22 自建 GitLab 会挤爆 100G/24G（补 GitHub Actions + self-hosted runner 等三方案 + docker.sock vs DinD）；求职无 JD 反推。

**D 类 口径 5 处**：440h 应为"18~22h/周，真正动手仅 270~290h"；provider 名 `aliyun/alicloud`；`terraform destroy` 对包年包月只销毁不退费；W21 D6"3 小时复现架构"不现实；预算上调至 450~1600 元。

**E 类 方向性 1 处（最重要）**：目标岗位"云交付/云实施"= 出差最多的形态，**与用户"不喜欢现场实施、想少出差"的取向直接冲突** → 改首选"云平台运维 / 云原生运维 / 运维开发(初级) / SRE(初级)"，"云实施"列入避开清单。

### 方法论沉淀
- 审阅技术学习计划时，**先联网核实时间敏感事实**（组件退役状态、云产品限制、证书周期、镜像源可用性），这类内容最容易过时且最容易误导。
- 缺陷按「事实错误 / 自相矛盾 / 缺失环节 / 口径过时 / 方向性偏差」五类归档，比逐条列问题更易被接受。
- 该文档 v1 的问题密度高（平均每 37 行一处），说明**规划类文档必须由对口专业视角过一遍**，通用视角写出来的计划会在"具体产品限制"和"已退役技术"上出大错。

---

## 任务二：清理 v1 痕迹 + 转为"老师带学"模式

### 已做的两件事
1. **清掉文档里全部 v1/v2 对比痕迹**（用户要求"删掉 V1 版本的计划"）。
   - 做法：Python 脚本做 70 处精确字符串替换 + 按行删除头部说明块 + 整节删除「修订记录」。
   - 原则：**删的是"v1 怎么错/v2 怎么改"这类元话语，技术内容一字不动**（否则等于把修正成果扔掉）。
   - 结果：1480 行，`v1 ` 出现 0 次，仅剩 2 处 `v2`（都是 `cgroup v2` 这个内核特性名，合法）。
   - 脚本用完即删（`_cleanup.py`）。
2. **新建 `学习日志.md`** 作为教学载体：进度总览表 + 每课结构（这一课解决什么 / 任务 / 预期输出 / 我的实测输出 / 解读 / 卡点 / 要记的三件事）。用户只需把命令输出贴回来，我负责解读和记录。

### 教学模式约定（重要，后续每课都按这个来）
- **每课只推一小步**，绝不提前灌后面的内容。
- **不生硬**：先给真实场景（"运维接手一台机器第一件事是什么"），再给命令，再给"预期长什么样"让他自查，最后才是原理。
- **不追问开放问题**：给具体命令 + 预期输出 + 分支处理（"如果输出 X 就是虚拟机，如果 none 就是物理机"），符合用户"给选项比提问有效"的习惯。
- 结构固定：**任务 → 预期输出 → 用户实测 → 我的解读 → 卡点 → 三件事**。
- 第 1 课 = W1 D1 体检 + 第 0 步形态确认，用 `systemd-detect-virt` 一条命令判断虚拟化形态（比翻设置优雅）。

### 排课顺序（前 4 课，注意第 4 课有硬性时序）
1. 认清机器（体检）
2. 装工具 + Docker（含"不重新登录导致 docker 权限报错"的坑，故意踩一次）
3. SSH + 固定 IP
4. **备份与快照 —— 必须排在所有"故意搞坏"练习之前**，否则 W2 改坏 sudoers 就是真事故

